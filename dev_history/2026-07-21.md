# 개발일지 — 2026-07-21

## 오늘의 결과 한 줄

스케줄링 API 서버 통로(Phase 1)와 데이터 저장소(Phase 2)를 완성했다. 두 번의 코드리뷰(각 88% 통과)와 그 지적사항 수정까지 끝났다. develop에 로컬 커밋 22개, push는 하지 않았다.

---

## 흐름 1 — 시작: 어디까지 됐는지 점검

- 나흘 만의 세션. PROGRESS·TASK·git log 삼자 대조로 상태 확인 — "자동배정 계산 엔진까지만 완성, 서버 통로·저장소·화면은 없음"을 재확인.
- 루트에 `tmp_verify.py;C`라는 정체불명 항목 발견 → 조사 결과 7/20 검증 작업 중 명령어가 잘못 끊겨 생긴 **빈 폴더**(git 무추적, 무해)로 판명 → 사용자 승인 후 삭제.

## 흐름 2 — 로드맵 결정

- 남은 작업을 페이즈로 정리해 사용자 승인: **토대(저장소+API) → 로그인 → 화면 → LLM → 배포**, 엔진 다듬기는 병렬 트랙.
- Phase 1 시작점 갈림길에서 사용자 결정: **"서버 통로(API) 먼저"** — 저장소 없이 요청 한 번에 팀·방·개수를 통째로 받아 엔진을 노출. 가장 빨리 "돌아가는 것"을 확보하는 쪽을 택함.

## 흐름 3 — Phase 1: 스케줄링 API (커밋 54ab911 ~ 0bc96ec)

- 계획서(`docs/superpowers/plans/2026-07-21-scheduling-api.md`) 작성 후 서브에이전트 7 task로 TDD 실행.
- 원칙 두 개를 지킴: **엔진 소스 동결**(위에 얇은 통로만), **검증 재구현 금지**(엔진의 ValueError 계약을 422로 전달만).
- 배정 실패는 오류가 아니라 정상 결과 → **200 + feasible:false + 조율안**. 422는 요청 자체의 잘못, 500은 서버 고장 — 이 3분류가 이 API의 뼈대.

### 트러블슈팅 ① — 8000 포트 충돌 (육하원칙)

- **누가/무엇**: Task 6(컨테이너 서버 기동)의 서브에이전트가 `docker compose up -d api` 검증 단계에서.
- **언제/어디서**: 라이브 검증 시도 시, 호스트 8000 포트에서.
- **왜**: 이 저장소와 무관한 다른 프로덕트(PoC_Asterix)의 `infrastructure` 컴포즈(gateway 컨테이너)가 세션 시작 전부터 8000을 점유 중이었음.
- **어떻게**: 규율(CLAUDE.md 4-1)에 따라 임의로 남의 스택을 내리지 않고 중단·보고. **사용자 결정: "이 PC에는 프로덕트별 컴포즈가 있으니 그 컴포즈를 내리고 8000으로 올려라"** → `docker compose -p infrastructure down` 후 검증 완료. 해당 스택은 지시대로 재기동하지 않음.

### 트러블슈팅 ② — Git Bash 한글 curl 인코딩

- Git Bash에서 한글 포함 JSON을 `curl -d '...'` 인라인으로 보내면 인코딩이 깨져 서버가 못 읽음 → UTF-8 파일 저장 후 `curl --data-binary @파일`로 우회 (상세: `.superpowers/sdd/task-6-report.md`).

## 흐름 4 — Phase 1 코드리뷰와 사용자 결정 (커밋 52221e3 ~ 60226cf)

- 전체 브랜치 리뷰 88% 통과 (Critical 0 / Important 2 / Minor 4). **사용자 결정: "6건 전부 고쳐라. 한 스텝씩 설명하고 승인받아라."**
- 수정 ② 상한값은 사용자가 직접 조정: **팀 20 / 팀당 멤버 10 / 합주실 10 / 불가능시간 100**. 팀당 자리 개수는 "집중기간 길이를 모르면서 보수적으로 잡지 말라"는 지적에 따라 고정 상한 대신 **동적 검사**(팀 수 × 요청 칸 > 운영시간의 전체 칸 수 → 사전 거부)로 확정.
- 수정 6건: 422 범위 축소(창구 안으로) / 상한+동적 검사 / 422 응답 모양 문장 하나로 통일 / 남는자리 테스트(일부러 망가뜨려 잡히는지까지 확인) / 컴포즈 죽은 줄 제거 / 타입 표기.

### 사용자 질의 — 컨테이너 안 가상환경의 정체

- "컨테이너가 격리인데 왜 venv냐"는 질문. 결론: **격리용이 아니라 의존성 설치 폴더**. `/app`은 소스 폴더가 덮어씌우므로 바깥에 두는 것이고, uv sync가 가상환경 형식 폴더를 대상으로 동작할 뿐. 이름이 오해를 부른다는 지적에 따라 **사용자 결정: `/opt/venv` → `/opt/dependency` 개명** (커밋 18ffb4c, 재빌드 검증 완료).

## 흐름 5 — 이해도 검증 퀴즈

- 사용자 요청으로 기획+구현 전체를 퀴즈로 검증. 여러 판 진행.
- 유일한 개념 교정 1건: **"배정 불가능 = 500이 아니라 200"** (실패는 정상 결과라는 설계 철학). 나머지는 전부 정답 — 명칭 두 개(소셜 로그인, "매일" 옵션)만 짚음.
- 사용자가 퀴즈 중 짚은 유효한 한계: **현 엔진은 이름 글자가 곧 사람이라 동명이인을 구분 못 함** → DB 설계에 반영됨(사람 식별자 = id, 이름 고유조건 없음).

## 흐름 6 — Phase 2: 데이터 저장소 (커밋 d9be055 ~ 423e4e4)

- 계획서(`docs/superpowers/plans/2026-07-21-database-layer.md`) 작성, 5 task 실행.
- PostgreSQL 17 컨테이너(호스트 포트 안 엶 — 이 PC의 타 프로덕트 postgres와 충돌 원천 차단), 테이블 8개, Alembic 마이그레이션, 실제 DB에 마이그레이션을 적용해 도는 테스트.
- 포지션 기본 5종(보컬·기타·베이스·드럼·키보드)은 코드가 아닌 **데이터 시드**로 — 목록 변경에 코드 수정 불요.

### 트러블슈팅 ③·④ — 테스트 DB 픽스처 (상세: `.superpowers/sdd/db-task-2-report.md`)

- ③ 마이그레이션 실행기(env.py)가 환경변수 주소를 무조건 우선해, 테스트 픽스처가 지정한 `banblit_test` 주소가 무시되고 메인 DB에 조용히 no-op 적용 → 호출자 명시 주소(config.attributes) 최우선으로 수정.
- ④ drop_all이 alembic 이력 테이블을 안 지워 연속 실행 시 "이미 최신"으로 판단, 스키마 미생성 → 세션 시작 시 이력 테이블도 삭제.
- 계획서 예시 코드의 버그였음. 서브에이전트가 systematic-debugging으로 원인 규명 후 수정 — 이후 task들이 이 수정을 유지하도록 장부에 명시.

## 흐름 7 — DB 리뷰와 사용자 정책 결정 (커밋 d77e146)

- 리뷰 88% 통과 (Critical 0 / Important 4). "미룰 것 3건"으로 브리핑하자 **사용자가 그 자리에서 전부 정책 확정**:
  - **삭제 정책**: "인원이 탈퇴하면 당연히 일정도 삭제" → 소속·불가능시간·배정은 CASCADE, 포지션 참조만 RESTRICT. 부수 결정: **팀 있는 멤버 탈퇴 시 헤드매니저에게 파트 대체자 지정 알림**(알림 기능 생긴 뒤 구현, TASK 등재). "무소속인 사람은 애초에 불가능시간을 넣을 이유가 없다"는 논거.
  - **시각 순서**: "닫는 시각이 여는 시각보다 늦은 건 당연" → rooms CHECK 추가.
  - **시간 겹침**: "각 칸마다 점유 플래그, 선착순 1이면 그 시간 예약 불가" → **모든 점유는 30분 칸 단위·칸당 1개** 구조로 확정. 어긋난 구간 겹침은 구조적으로 불가능해져 별도 장치 불요. 예약 테이블도 같은 구조로 만들기로(TASK 등재).
  - **연산 시각**: "기간 종류와 무관하게 무조건 지정" → 두 컬럼 NOT NULL.

## 소통 방식 교정 (이 세션에서 확립, 메모리에 영구 기록)

- **결론 먼저**: 바뀌는 동작 한 문장을 맨 앞에, 배경은 뒤에. 빙 돌아 설명 금지.
- **질문은 스스로 의도를 밝혀야 함**: 용어를 쓰려면 그 용어가 뭘 가리키는지 질문 안에 정의. 추상적 질문 금지, 구체 상황으로.
- **규칙에 이미 정해진 것은 묻지 않는다**: 실행 방식(서브에이전트) 같은 건 CLAUDE.md에 있으므로 선택지로 제시하지 말 것.

## 마감 시점 상태

- develop 로컬 커밋 22개 (229ce73 이후), **push 미실행**.
- 테스트 66개 전체 통과 (`docker compose run --rm dev pytest -q`).
- 진행 장부: `.superpowers/sdd/progress.md` / task별 보고서·리뷰: `.superpowers/sdd/*.md`
- 다음 후보(사용자 미결정): **DB→엔진 연결**(저장된 데이터로 배정 호출) 또는 **인증**(이메일+비번, 소셜, JWT).
