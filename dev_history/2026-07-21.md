# 개발일지 — 2026-07-21

## 오늘의 결과 한 줄

스케줄링 API 서버 endpoint(API 의 요청 주소 단위, Phase 1)와 데이터 저장소(Phase 2)를 완성했습니다. 두 번의 코드리뷰(각 88% 통과)와 그 지적사항 수정까지 완료했습니다. develop에 로컬 커밋 22개, push는 하지 않았습니다.

---

## 흐름 1 — 시작: 어디까지 됐는지 점검

- 나흘 만의 세션입니다. PROGRESS·TASK·git log를 세 가지로 대조해 상태를 확인했습니다. 자동배정 계산 엔진까지만 완성되고, 서버 endpoint·저장소·화면은 없음을 재확인했습니다.
- 루트에 `tmp_verify.py;C`라는 정체불명 항목을 발견했습니다. 조사 결과 7월 20일 검증 작업 중 명령어가 잘못 끊겨 생긴 **빈 폴더**(git 무추적, 무해함)로 판명되었습니다. 사용자 승인 후 삭제했습니다.

## 흐름 2 — 로드맵 결정

- 남은 작업을 단계(phase)로 정리해 사용자 승인을 받았습니다: **토대(저장소+API) → 로그인 → 화면 → LLM → 배포**, 엔진 다듬기는 병렬 실행합니다.
- Phase 1 시작점 갈림길에서 사용자가 결정했습니다: **서버 endpoint(API 의 요청 주소 단위)를 먼저 구현합니다.** 저장소 없이 요청 한 번에 팀·방·개수를 통째로 받아 엔진을 노출합니다. 가장 빨리 돌아가는 것을 확보하는 쪽을 선택했습니다.

## 흐름 3 — Phase 1: 스케줄링 API (커밋 54ab911 ~ 0bc96ec)

- 계획서(`docs/superpowers/plans/2026-07-21-scheduling-api.md`)를 작성한 후 서브에이전트 7개 task로 TDD를 실행했습니다.
- 원칙 두 개를 지켰습니다: **엔진 소스 동결**(위에 얇은 endpoint만), **검증 재구현 금지**(엔진의 ValueError 계약을 422로 전달만 합니다).
- 배정 실패는 오류가 아니라 정상 결과입니다. **200 + feasible:false + 조율안**을 반환합니다. 422는 요청 자체의 잘못, 500은 서버 고장입니다. 이 3가지 분류가 이 API의 뼈대입니다.

### 트러블슈팅 ① — 8000 포트 충돌 (육하원칙)

- **누가/무엇**: Task 6(컨테이너 서버 기동)의 서브에이전트가 `docker compose up -d api` 검증 단계에서 발견했습니다.
- **언제/어디서**: 라이브 검증을 시도할 때, 호스트 8000 포트에서입니다.
- **왜**: 이 저장소와 무관한 다른 프로덕트(PoC_Asterix)의 `infrastructure` compose(gateway 컨테이너)가 세션 시작 전부터 8000을 점유하고 있었습니다.
- **어떻게**: 규율(CLAUDE.md 4-1)에 따라 임의로 남의 스택을 내리지 않고 중단·보고했습니다. **사용자가 결정했습니다: 이 PC에는 프로덕트별 compose가 있으니 그 compose를 내리고 8000으로 올려라.** `docker compose -p infrastructure down` 후 검증을 완료했습니다. 해당 스택은 지시대로 재기동하지 않았습니다.

### 트러블슈팅 ② — Git Bash 한글 curl 인코딩

- Git Bash에서 한글을 포함한 JSON을 `curl -d '...'` 인라인으로 보내면 인코딩이 손상되어 서버가 읽지 못합니다. UTF-8 파일을 저장한 후 `curl --data-binary @파일`로 우회했습니다 (상세: `.superpowers/sdd/task-6-report.md`).

## 흐름 4 — Phase 1 코드리뷰와 사용자 결정 (커밋 52221e3 ~ 60226cf)

- 전체 브랜치 리뷰 88% 통과 (Critical 0 / Important 2 / Minor 4). **사용자가 결정했습니다: 6건을 전부 고쳐라. 한 단계씩 설명하고 승인을 받아라.**
- 수정 항목 ②의 상한값은 사용자가 직접 조정했습니다: **팀 20 / 팀당 멤버 10 / 합주실 10 / 불가능시간 100**. 팀당 자리 개수는 집중기간 길이를 모르면서 보수적으로 잡지 말라는 지적에 따라 고정 상한 대신 **동적 검사**(팀 수 × 요청 slot > 운영시간의 전체 slot 수 → 사전 거부)로 확정했습니다.
- 수정 6건: 422 범위 축소(창구 안으로) / 상한+동적 검사 / 422 응답 형식을 문장 하나로 통일 / 빈 자리 테스트(의도적으로 망가뜨려 감지하는지까지 확인) / compose 죽은 줄 제거 / 타입 표기.

### 사용자 질의 — 컨테이너 안 가상환경의 정체

- 컨테이너가 격리인데 왜 venv냐는 질문이 있었습니다. 결론: **격리용이 아니라 의존성 설치 폴더**입니다. `/app`은 소스 폴더가 덮어씌우므로 바깥에 두는 것이고, uv sync가 가상환경 형식 폴더를 대상으로 동작할 뿐입니다. 이름이 오해를 부른다는 지적에 따라 **사용자가 결정했습니다: `/opt/venv` → `/opt/dependency` 개명** (커밋 18ffb4c, 재빌드 검증 완료했습니다).

## 흐름 5 — 이해도 검증 퀴즈

- 사용자 요청으로 기획+구현 전체를 퀴즈로 검증했습니다. 여러 판을 진행했습니다.
- 유일한 개념 교정 1건: **배정 불가능 = 500이 아니라 200입니다.** (실패는 정상 결과라는 설계 철학) 나머지는 전부 정답이었습니다. 명칭 두 개(소셜 로그인, 매일 옵션)만 지적했습니다.
- 사용자가 퀴즈 중 지적한 유효한 한계: **현 엔진은 이름 글자가 곧 사람이라 동명이인을 구분하지 못합니다.** DB 설계에 반영되었습니다(사람 식별자 = id, 이름은 고유조건 없음).

## 흐름 6 — Phase 2: 데이터 저장소 (커밋 d9be055 ~ 423e4e4)

- 계획서(`docs/superpowers/plans/2026-07-21-database-layer.md`)를 작성하고, 5개 task를 실행했습니다.
- PostgreSQL 17 컨테이너(호스트 포트를 열지 않음. 이 PC의 다른 프로덕트 postgres와 충돌을 원천 차단합니다), 테이블 8개, Alembic migration(DB 구조를 바꾸는 단계별 기록), 실제 DB에 migration을 적용해서 실행하는 테스트를 작성했습니다.
- 포지션 기본 5종(보컬·기타·베이스·드럼·키보드)은 코드가 아닌 **데이터 시드**로 관리합니다. 목록 변경에 코드 수정이 필요 없습니다.

### 트러블슈팅 ③·④ — 테스트 DB 픽스처 (상세: `.superpowers/sdd/db-task-2-report.md`)

- ③ migration(DB 구조를 바꾸는 단계별 기록) 실행기(env.py)가 환경변수 주소를 무조건 우선해, 테스트 fixture가 지정한 `banblit_test` 주소가 무시되고 메인 DB에 오류 메시지 없이 적용 안 함으로 처리되었습니다. 호출자 명시 주소(config.attributes)를 최우선으로 수정했습니다.
- ④ drop_all이 alembic 이력 테이블을 지우지 않아 연속 실행 시 이미 최신이라고 판단하고, schema를 생성하지 않았습니다. 세션 시작 시 이력 테이블도 삭제하도록 수정했습니다.
- 계획서 예시 코드의 버그였습니다. 서브에이전트가 systematic-debugging으로 원인을 규명한 후 수정했습니다. 이후 task들이 이 수정을 유지하도록 장부에 명시했습니다.

## 흐름 7 — DB 리뷰와 사용자 정책 결정 (커밋 d77e146)

- 리뷰 88% 통과 (Critical 0 / Important 4). 미룰 사항 3건으로 브리핑하자 **사용자가 그 자리에서 전부 정책을 확정**했습니다:
  - **삭제 정책**: 인원이 탈퇴하면 당연히 일정도 삭제됩니다. 소속·불가능시간·배정은 CASCADE, 포지션 참조만 RESTRICT입니다. 부수 결정: **팀이 있는 멤버가 탈퇴할 때 헤드매니저에게 파트 대체자 지정 알림을 보냅니다.**(알림 기능이 생긴 뒤 구현, TASK 등재). 소속이 없는 사람은 애초에 불가능시간을 넣을 이유가 없다는 논거입니다.
  - **시각 순서**: 종료 시각이 시작 시각보다 늦은 것은 당연합니다. rooms 테이블에 CHECK 제약을 추가했습니다.
  - **시간 겹침**: 각 slot마다 점유 flag(표시), 선착순 1이면 그 시간 예약 불가로 합니다. **모든 점유는 30분 slot 단위·slot당 1개** 구조로 확정했습니다. 다른 구간이 겹친 경우는 구조적으로 불가능해져 별도 장치가 필요 없습니다. 예약 테이블도 같은 구조로 만들기로 했습니다(TASK 등재).
  - **계산 시각**: 기간 종류와 무관하게 반드시 지정해야 합니다. 두 컬럼을 NOT NULL로 설정했습니다.

## 소통 방식 교정 (이 세션에서 확립, 메모리에 영구 기록)

- **결론 먼저**: 바뀌는 동작을 한 문장으로 맨 앞에 놓고, 배경은 뒤에 둡니다. 빙 돌아 설명하지 않습니다.
- **질문은 스스로 의도를 밝혀야 합니다**: 용어를 사용하려면 그 용어가 무엇을 가리키는지를 질문 안에 정의합니다. 추상적 질문은 금지하고, 구체 상황으로 제시합니다.
- **규칙에 이미 정해진 것은 묻지 않습니다**: 실행 방식(서브에이전트) 같은 것은 CLAUDE.md에 있으므로 선택지로 제시하지 말아야 합니다.

## 마감 시점 상태

- develop 로컬 커밋 22개 (229ce73 이후), **push를 하지 않았습니다.**
- 테스트 66개 전체 통과했습니다 (`docker compose run --rm dev pytest -q`).
- 진행 장부: `.superpowers/sdd/progress.md` / task별 보고서·리뷰: `.superpowers/sdd/*.md`
- 다음 후보(사용자가 미결정): **DB→엔진 연결**(저장된 데이터로 배정 호출) 또는 **인증**(이메일+비번, 소셜, JWT)입니다.
