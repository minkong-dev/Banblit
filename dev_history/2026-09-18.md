# 2026-09-18

## 지금 상태

**`AUDIT.md` 1장(결함 11건)과 4장(문체·주석) 대부분, 2-4 의 index, 2-6 의 죽은 코드를 처리했습니다.**
무엇을 수정했고 무엇이 남았는지는 `AUDIT.md` 맨 아래 "처리 결과" 표가 정본입니다.

**다음 한 걸음은 개발자님의 결정 8건입니다**(맨 끝 "남은 것" 2번). 그 결정이 나와야 구조 작업(대형 파일 절단·
중복 통합)의 범위가 정해집니다.

**dev DB 에 새 migration(`3c9d41b7e2a5`)을 아직 적용하지 않았습니다.** 적용 명령은
`docker compose run --rm dev alembic upgrade head` 입니다. 적용 전에는 dev DB 에 배정 table 의 period_id index 가 없습니다.

검사 상태: pytest 602 통과, mypy 0, 화면 테스트 332 통과, 화면 타입 검사 통과, `docker compose build dev` 성공.
화면 lint 는 7건 실패합니다 — 전부 상호작용을 변경하는 수정이라 결정 대기입니다. E2E 는 실행하지 않았습니다.

## 용어

| 말 | 뜻 |
|---|---|
| 점유 단위 | 예약·배정이 차지하는 시간 칸의 길이(분). 설정의 `slot_minutes` 이고 5·10·12·15·20·30·60 중 하나 |
| reverse proxy | 외부 요청을 먼저 받아 내부 서비스로 전달하는 서버. 배포에서는 Caddy 와 nginx 2개 |
| X-Forwarded-For | 요청이 거쳐 온 주소를 쉼표로 나열한 HTTP 헤더. reverse proxy 가 각자 관찰한 주소를 맨 뒤에 추가 |
| queryKey | TanStack Query(서버 조회 결과를 보관하는 라이브러리)가 조회 1개를 구분하는 값. 무효화하면 다시 조회 |
| RED | TDD(테스트 주도 개발)에서 구현 전에 테스트가 실패하는 것을 직접 확인하는 단계 |

## 오늘 한 것

### 결함 11건 (`AUDIT.md` 1장)

전부 "오류 없이 틀린 결과를 내보내는" 항목입니다. 항목마다 실패하는 테스트를 먼저 쓰고 실패를 확인한 뒤 구현했습니다.

| # | 이전 | 지금 |
|---|---|---|
| 1-1 | 배포에서 모든 사용자의 로그인 시도 횟수가 Caddy 컨테이너 주소 1개로 합산 | `TRUSTED_PROXY_COUNT`(배포 2·개발 1)만큼 뒤에서 센 주소를 요청자로 사용 |
| 1-2 | `+09:00` 이 붙은 시각이 통과해 9시간 어긋난 예약이 생성 | 요청 모델 3개를 pydantic `NaiveDatetime` 으로 변경, 422 로 거부 |
| 1-3 | 블라인드를 해제해도 게시판 목록에 글이 돌아오지 않음 | `BOARD_KEY`·`boardListKey`(`lib/boards.ts`)로 queryKey 를 한 곳에서 생성 |
| 1-4 | 30분 단위에서 18:00–18:30 예약이 18:30–19:00 을 막음 | `takenGrid`·`isRangeFree`·`acceptsDrag` 에 `slotMinutes` 추가, `firstTaken` 신설 |
| 1-5 | 18:30 에 개방하는 합주실이 0시간으로 계산 | `capacity`·`openingHours` 가 점유 단위를 받음 |
| 1-6 | 화면은 60초에 "실패"를 표시하는데 서버는 계속 계산해 나중에 반영 | 화면 상한 390초(서버 300+60초에 여유 30초) |
| 1-7 | 이름·기수를 다른 사람과 같게 수정하면 무관한 문구 | "이미 가입된 사람입니다"(가입과 같은 422) |
| 1-8 | 조회 실패 문구에 `Error:` 가 그대로 출력 | `reason()` 사용 |
| 1-9 | 합주실 0개일 때 "잠시 후 다시 시도" | "예약할 합주실이 없어요…" |
| 1-10 | 배정 작업이 `done` 인데 `finished_at` 이 없는 응답 가능 | `finished_at` 을 Future 보다 먼저 기록 |
| 1-11 | 수정하지 않음 | `teams.name`·`rooms.name` 이 UNIQUE 라 같은 이름의 팀 2개가 생성될 수 없습니다 |

**`AUDIT.md` 의 기술이 틀린 곳이 2건 있었습니다.** 1-5 는 "절반으로 표시"가 아니라 60분 격자에 맞지 않는 합주실이
0칸으로 계산되는 것이었고, 1-11 은 DB 제약 때문에 발생할 수 없는 시나리오였습니다. 전수조사 결과도 코드와 대조한 뒤에 수정해야 합니다.

**1-7 에서 테스트가 틀렸습니다.** 처음에 409 를 기대했는데 가입(`POST /signup`)은 같은 제약 위반에 422 를 반환합니다.
구현이 아니라 테스트를 기존 규약(422)에 맞췄습니다.

### 날짜 모달의 "내 일정" 목록 — 개발자님이 강하게 지적한 항목

**모달의 "불가능 일정 목록"은 그날 항목이 아니라 내가 등록한 불가능 일정 전부를 나열합니다.** 개발자님이 30번 넘게
지시한 사항인데 "그날 항목만" 으로 구현되어 있었습니다. 게다가 이것을 "문서와 동작의 불일치"로 분류하고 의도를 되물어
지적받았습니다. 원인은 두 가지입니다 — `AUDIT.md` 의 분류를 그대로 옮겼고, 되묻기 전에 개발자님의 이전 지시를 확인하지 않았습니다.

`lib/dayEntries.ts` 에 `allOffEntries`(전부를 시작 시각 순으로, 반복 일정은 전개하지 않고 1줄)와 `offWhenLabel`
("9월 14일 월요일 · 매주")을 추가했습니다. 줄에 날짜가 붙어 길어졌으므로 모달 폭에서의 줄바꿈은 QA 에서 확인해야 합니다.
"내 예약" 목록은 변경하지 않았습니다(그날의 예약만).

이 지시는 메모리(`feedback-my-off-list-is-all`)에 기록했습니다.

### 목록을 코드와 대조하지 않고 제시한 것 — 지적받음

"남은 것"을 물으셨을 때 `AUDIT.md` 의 목차만 보고 목록을 적었다가 "탐색도 안 하고 말을 하느냐"는 지적을 받았습니다.
그 뒤 항목별로 `grep`·`wc` 로 현재 코드와 대조했고, 전부 미해결로 확인됐습니다. **문서에 적힌 상태를 현재 상태로 전달하지 않습니다.**

### 문체·주석·죽은 코드 (`AUDIT.md` 4장·2-6·2-4)

- 금지된 순우리말 동사(지우다·고치다·바꾸다·고르다·비우다·정하다) 약 100줄을 한자어 동사로 치환했습니다. 화면 문구도 포함됩니다
  ("시각을 골라요" → "시각을 선택해요"). "고르게"(균등하게)는 다른 뜻이라 제외했습니다
- "창 길이"→window 길이, "정상 확인"→health check, "떨구다"→drop, "재생기"→audio player·video player
- 실제와 다른 주석: `SLOT_MINUTES`, "18가지 항목", `login_sessions`, `room_manage`, "1시간 단위 시간 칸" 13곳
- 의인화 표현 9줄 수정, `(사용자 결정 …)` 괄호 22곳 삭제
- 죽은 코드 `sideExtra`·`writeNote`·`calbody`·`.picks`·`.posbad` 삭제, `BLINDED_KEY` 를 `lib/boards.ts` 로 이동
- migration `3c9d41b7e2a5` — `assignments(period_id)`, `assignment_backups(period_id, saved_at)` index
- `backend/Dockerfile` 의 `uv sync` 3곳에 캐시 마운트

## 트러블슈팅

| 언제·어디서 | 무엇이 | 왜 | 어떻게 |
|---|---|---|---|
| 세션 시작, 호스트 | `docker compose run` 이 API 에 연결하지 못함 | Docker Desktop 이 꺼져 있었음 | Docker Desktop 을 기동 |
| 1-4 작업 중, 호스트 | 파일을 수정하는 `node -e` 스크립트가 120초 동안 멈춤 | 호스트에 node·python 이 없음(모든 실행은 컨테이너 안) | 작업을 중단하고 Edit·`sed`·`perl` 로 수정. 호스트에서 쓸 수 있는 스크립트 언어는 Git Bash 의 `perl` 뿐 |
| index 작업 중, `models.py` | index 가 `unavailable_times` 에 잘못 붙음 | 같은 `__table_args__` 문장이 두 table 에 있는데 치환이 첫 번째에 적용됨 | 즉시 되돌리고 `assignment_backups` 구간으로 한정해 다시 치환. `awk` 로 table 별 확인 |
| 리뷰 후, `SchedulerViews.tsx` | 달 보기 막대 수가 점유 단위에 비례해 증가(5분 단위 144개) | 점유 판정용 grid 를 막대 그리기에도 사용 | 막대는 1시간 칸 grid 로, 남은 시간·마감 판정만 점유 단위 grid 로 분리 |

## 검증

| 대상 | 테스트 파일 | 실행 명령 |
|---|---|---|
| 1-1 | `backend/tests/unit/test_rate_limit.py` | `docker compose run --rm --no-deps dev pytest -q tests/unit` |
| 1-2 | `backend/tests/unit/test_schemas.py` | 위와 같음 |
| 1-10 | `backend/tests/unit/test_job_runner.py` | 위와 같음 |
| 1-7 | `backend/tests/integration/db/test_auth_endpoints.py` | `docker compose run --rm dev pytest -q` |
| index | `backend/tests/integration/db/test_migration_chain.py` | 위와 같음 |
| 1-3 | `frontend/src/lib/boards.test.ts` | `docker compose run --rm --no-deps web npm test` |
| 1-4 | `frontend/src/lib/calendar.test.ts` | 위와 같음 |
| 1-5 | `frontend/src/lib/settings.test.ts`, `pipeline.test.ts` | 위와 같음 |
| 1-6 | `frontend/src/lib/jobs.test.ts` | 위와 같음 |
| 모달 목록 | `frontend/src/lib/dayEntries.test.ts` | 위와 같음 |

리뷰는 `git diff` 범위로 2건 실행했습니다. `react-reviewer` 5/5 통과(MEDIUM 1건 — 위 막대 수, 수정함),
`security-reviewer` 4/4 통과(MEDIUM 1건 — `TRUSTED_PROXY_COUNT` 가 누락되면 경고 없이 1로 동작. `docker-compose.yml` 에
2를 명시해 두어 수정하지 않음). 문체 치환·죽은 코드·index 는 리뷰를 실행하지 않았습니다.

## 남은 것

### 1. 개발자님이 하실 것

- dev DB 에 migration 적용 — `docker compose run --rm dev alembic upgrade head`
- `.env` 에 `ADMIN_SIGNUP_CODE` 입력 후 서버 재시작
- 수동 QA — 점유 단위 30분 상태의 예약, 모달의 불가능 일정 목록(줄 길이), 본문의 소리·PDF·유튜브, 치환된 화면 문구
- banblit.com 을 Cloudflare proxy 로 운영하면 reverse proxy 가 3개가 됩니다. `TRUSTED_PROXY_COUNT` 와 Caddy 의
  `trusted_proxies` 를 함께 변경해야 합니다(`.cluedoc/deployment/README.md`)

### 2. 개발자님 결정이 필요한 것

| 항목 | 결정할 내용 |
|---|---|
| lint 7건 | `autoFocus` 4건 삭제 여부, `Landing.tsx:99` 클릭 요소, `Account.tsx:140` label |
| 같은 글을 두 사람이 수정하면 나중 저장이 앞선 수정을 덮어씀 | 수정할지 (`PostBoard.tsx`, `SettingsAccount.tsx:40`) |
| 팀 자리 저장이 중간에 실패하면 일부만 반영 | 수정할지 (`Teams.tsx` 의 `writeSeats`) |
| 게시판·알림 목록에 개수 상한이 없음 | 페이지 단위 조회를 넣을지 |
| 첨부 총량 상한이 없음 | 상한을 둘지, 값은 얼마인지 |
| 합주실 삭제 기능이 없음 | 만들지 |
| 점유 단위 6분 — DB CHECK 는 허용, API 는 거부 | 어느 쪽에 맞출지 |
| 기수가 없을 때 표기 | "기수 없음"과 빈칸 중 선택 |

### 3. 결정 없이 할 수 있는 것

- E2E 실행(21개)
- 로딩 문구 7가지 통일, "큐/대기열/queued" 통일, 위치가 어긋난 주석 3곳, 밖에서 쓰이지 않는 `export` 5개
- `Scheduler.tsx` 의 달력 집계에 `useMemo`
- `CLAUDE.md` 6장의 "점유 단위는 한 시간 칸 고정"·`SLOT_MINUTES` 표기가 현재 코드와 어긋납니다(자동 수정 대상이 아님)

### 4. 구조 작업 (`refactoring` 스킬로 진행)

- 대형 파일 3개 절단 — `PostBoard.tsx` 808줄, `Settings.tsx` 710줄, `Teams.tsx` 562줄
- 중복 코드 통합 — `window.confirm` 직접 호출 3곳, `MINUTES_PER_HOUR` 4곳 등
- 라우터의 DB 직접 쓰기를 서비스로 이동 — `routers/auth.py`, `routers/boards.py`
- 접근성 — `Field.tsx` 의 오류 읽기, `Tabs` 의 화살표 키, 닫힌 popover 에 초점이 가는 문제

### 5. 확인하지 못한 것

`AUDIT.md` 3장의 "주석이 없어서 생긴 질문 약 250건"은 개수를 셀 기준이 없어 현재 상태를 검증하지 못했습니다.
