# 2026-09-16

## 지금 상태

**기능 작업이 전부 끝났습니다. 다음 한 걸음은 개발자님의 QA 입니다.** 본문에 넣은 소리·PDF·
유튜브를 실제로 눌러 보는 것이 남았습니다(E2E 는 그림만 지나갑니다).

그 전에 `.env` 에 `ADMIN_SIGNUP_CODE` 를 적어야 합니다. 적지 않으면 새로 세우는 저장소에서
관리자 계정을 만들 수 없습니다. 지금 dev DB 에는 헤드매니저 계정이 이미 있어 당장은 영향이 없습니다.

- 검사: pytest 593 통과, mypy `src` 오류 0, 화면 테스트 322 통과, 화면 타입 검사·lint·lint:e2e
  통과, E2E 21 통과
- 미추적 파일 `모달 현재.png`·`텍스트필드.png` 2개는 그대로 두었습니다
- 세션 진입점·명령·구조는 `dev_history/2026-09-15.md` 와 `COMMAND.md` 가 정본입니다

## 오늘 한 것

### 블라인드

부적절한 글을 처리할 방법이 삭제뿐이었습니다. 삭제는 되돌릴 수 없고 내용도 남지 않습니다.
지우지 않고 가리는 블라인드를 추가했습니다.

`posts.blinded_at` 한 열입니다. 값이 있으면 가려진 글이고 목록·상세에서 빠집니다. 작성자 본인도
볼 수 없습니다. 댓글에는 열을 두지 않았습니다 — 댓글은 글에 딸린 것이라 글이 가려지면 함께 가려집니다.
조회 조건이 전부 `blinded_at IS NULL` 이라 그 조건의 부분 index 를 함께 두었습니다.

- endpoint 3개를 추가했습니다 — `GET /blinded-posts`, `PUT /posts/{id}/blind`,
  `DELETE /posts/{id}/blind`. 전부 `board_moderate` 가 필요합니다
- 화면은 설정의 "블라인드" 탭 하나(`frontend/src/routes/SettingsBlinded.tsx`)와 글 상세의
  블라인드 버튼입니다. 탭은 예약 탭과 같은 구조라 새 모양을 만들지 않고 그대로 따랐습니다
- **남의 글 수정을 권한자에게도 막았습니다.** 전에는 서버가 허용하고 화면만 버튼을 감췄습니다.
  `update_post`·`update_comment` 가 `allow_moderator=False` 로 호출합니다
- **권한자는 팀 소속을 확인하지 않습니다.** 확인하면 그 팀에 속하지 않은 관리자가 부적절한 글을
  아무것도 처리할 수 없습니다. 삭제·블라인드·해제와 가려진 글 목록이 전부 소속을 보지 않습니다
- 검증: `backend/tests/integration/db/test_board_endpoints.py` 의 "===== 블라인드 =====" 아래 6개와
  `frontend/e2e/notices.spec.ts` 의 "공지를 블라인드하면 목록에서 사라지고 설정에서 되돌린다" 입니다.
  마이그레이션은 `alembic upgrade head` 와 `alembic downgrade -1` 을 실제로 실행해 확인했습니다
- 동작 규칙은 `.cluedoc/boards/README.md` 의 "블라인드는 지우지 않고 가립니다" 절에 적었습니다
- 리뷰에서 지적받은 "누가 가렸는지 기록이 없다" 를 같은 세션에서 반영했습니다. `posts.blinded_by_id` 를
  더하고 격리 목록에 "가린 사람" 칸을 두었습니다. 그 계정이 삭제되면 값만 비웁니다(ON DELETE SET NULL) —
  작성자처럼 CASCADE 로 두면 관리자 계정 하나를 지울 때 그 사람이 가린 글이 전부 삭제됩니다
- 리뷰(`python-reviewer`·`security-reviewer`)에서 두 검토자가 같은 구멍을 지적했습니다. 권한자가 팀 소속
  확인을 건너뛰게 되면서 `attachment_service.py` 의 업로드까지 그 우회를 물려받아, 권한자가 어느 팀의
  어느 글에도 파일을 붙일 수 있었습니다. 업로드만 우회를 끄고(`allow_moderator=False`) 테스트를 더했습니다.
  CRITICAL 없음. 남은 MEDIUM 은 "누가 가렸는지 기록이 없다" 1건입니다

### 배정 job 5분 상한

배정이 불가능하면 `resolve()` 가 멤버 1명씩 제외한 solver 계산을 멤버 수만큼 반복합니다.
solver 1회 상한이 60초라 멤버가 많으면 작업 1개가 몇 시간까지 갑니다.

`resolve()` 시작 시각에 5분을 더한 마감 시각을 잡고, 반복마다 그 시각을 넘겼는지 확인해
넘겼으면 `ValueError` 로 작업을 실패로 끝냅니다. 상수는
`backend/src/backend/scheduling/resolution.py` 의 `RESOLUTION_TIME_LIMIT_SECONDS` 입니다.

- 검증: `backend/tests/unit/test_resolution.py::test_fails_when_the_proposal_search_exceeds_the_time_limit`
  이 상한을 0초로 변경하고 배정 불가능한 입력을 넣어 `ValueError` 를 확인합니다.
  실행은 `docker compose run --rm --no-deps dev pytest -q tests/unit/test_resolution.py` 입니다
- 리뷰(`python-reviewer`)는 CRITICAL·HIGH·MEDIUM 없음. LOW 1건은 `resolve()` 의 docstring 부재로,
  이번 변경이 만든 것이 아니라 파일 전체의 기존 관례입니다
- 동작 규칙은 `.cluedoc/auto-assignment/README.md` 의 "계산 1회 전체에 5분 상한을 설정했습니다" 절에 적었습니다

### 권한 집합 race

모든 권한 항목을 가진 permission set(full set)이 2개 남은 상태에서 두 요청이 각각 1개씩 같은 순간에
삭제하면, 둘 다 "다른 1개가 있다" 를 확인하고 통과해 full set 이 0개가 됐습니다. 0개가 되면 권한을
부여할 사람이 사라지고 되돌릴 방법이 없습니다.

`backend/src/backend/services/permission_service.py` 의 `_require_another_full_set` 이 조회할 때
`with_for_update()` 로 full set 행 전체를 번호 오름차순으로 잠그도록 변경했습니다. `id != set_id`
필터와 `limit(1)` 을 빼고 파이썬에서 자기 자신을 제외합니다. 대상 자신도 함께 잠가야 두 요청이
같은 순서로 잠급니다 — 서로 다른 부분집합을 반대 순서로 잠그면 둘이 서로를 기다립니다.

- 검증: `backend/tests/integration/db/test_permission_endpoints.py::test_two_concurrent_deletes_keep_one_full_set`
  이 두 스레드를 barrier 로 같은 순간에 출발시켜 각각 1개씩 삭제하고, 남은 full set 이 1개인지와
  예외가 `ValueError` 1건인지를 확인합니다. 잠금을 빼면 0개가 되어 실패합니다.
  실행은 `docker compose run --rm dev pytest -q tests/integration/db/test_permission_endpoints.py` 입니다
- 리뷰(`python-reviewer`)는 CRITICAL·HIGH 없음. MEDIUM 2건(스레드 예외를 삼킴, `join` 시간 초과를
  실패로 만들지 않음)은 반영했고, LOW 2건 중 변수 이름 1건을 반영했습니다. 나머지 LOW 1건은
  `permissions` 열에 index 가 없어 조회가 순차 탐색이라는 것으로, 이번 변경 전에도 같았습니다
- 동작 규칙은 `.cluedoc/accounts-and-roles/README.md` 의 "이 검사는 조회한 행을 잠근 상태에서
  수행합니다" 절에 적었습니다

### 링크 미리보기

크롤러는 JavaScript 를 실행하지 않아 Vite 가 내보내는 `index.html` 만 읽습니다. 그 파일에 태그가
없어 카카오톡·페이스북에 주소를 붙여넣으면 제목·설명·이미지가 나오지 않았습니다.

`frontend/index.html` 에 `og:*` 와 `twitter:card` 를 넣었습니다. **서버(FastAPI)는 건드리지
않았습니다** — `index.html` 을 내보내는 것은 서버가 아니라 nginx 입니다(`frontend/nginx.conf.template`).
서버에 주입 코드를 넣으면 그 코드를 지나가는 요청이 없습니다.

`og:image` 는 절대 주소여야 하는데 정적 파일은 자기 주소를 실행 중에 알 수 없으므로, 묶는 시점에
박습니다. `.env` 의 `APP_ORIGIN` → `docker-compose.yml` 의 build arg → `frontend/Dockerfile` 의
`ENV` → Vite 가 `%VITE_APP_ORIGIN%` 을 교체하는 순서입니다. 주소를 새로 만들지 않고 비밀번호 재설정
링크가 쓰는 값을 그대로 쓴 이유는, 사용자가 접속하는 주소와 미리보기 주소가 갈라지지 않게 하기
위해서입니다. 이미지는 `/images/in_six_strings.jpg` 를 임시로 씁니다.

- 검증(개발 경로): `frontend/e2e/account.spec.ts` 의 "링크 미리보기 태그가 첫 화면에 들어 있다" 가
  `og:image` 가 `http://` 또는 `https://` 로 시작하고 `%` 가 남아 있지 않은지 확인합니다
- 검증(배포 경로): E2E 는 개발 서버만 보므로 `COMMAND.md` 10-4-1 을 추가했습니다. 배포용 image 를
  만들고 그 안의 `index.html` 에서 태그를 직접 읽습니다. 실행해 `og:url`·`og:image` 가
  `http://localhost:8080` 으로 채워지는 것을 확인했습니다(이 PC 의 `.env` 에 `APP_ORIGIN` 이 비어
  기본값이 사용됐습니다)

### 남은 작업 7건을 한 세션에 전부

개발자님이 "한번에 다 밀어" 로 지시해 아래 순서로 처리했습니다. 작업마다 커밋했습니다.

| 커밋 | 작업 | 내용 |
|---|---|---|
| `6e4f179` | 권한 구멍 | 추방과 남의 permission set 회수가 전체 권한 보유자를 0명으로 만들 수 있었습니다. `require_another_full_set_holder` 가 보유 행(`member_permission_sets`)을 계정 번호 오름차순으로 잠그고 셉니다 |
| `c5af208` | 드래그 중 찬 칸 | 예약 탭 드래그가 이미 찬 칸을 지나갈 수 있어 등록 시점에야 거절됐습니다. `lib/calendar.ts` 의 `acceptsDrag` 가 찬 칸이 걸치는 구간을 반영하지 않아 그 앞에서 멈춥니다 |
| `a1e8927` | 문서 불일치 | 집중 합주기간에 남은 칸을 예약할 수 있다고 적혀 있었으나 서버는 전부 거절합니다. `open_slots` 는 빈 자리를 알리는 값이고 화면이 쓰지 않습니다 |
| `32b0768` | 점유 단위 화면 | `PATCH /settings` 를 부르는 화면이 없어 endpoint 만 있었습니다. 합주실 탭에 카드를 두고 `room_edit` 권한일 때만 변경합니다 |
| `0254e52` | 배정 구간 한 행 | 칸마다 한 행이라 점유 단위 5분이면 하루치가 수백 행이 됩니다. 이어 쓰는 칸을 한 행으로 합치고, 겹침을 `(합주실, 시작 시각)` 중복 금지 대신 `EXCLUDE` 로 막습니다 |
| `64258b0` | 드롭다운 | 기본 `<select>` 13곳을 부품 하나로 통일했습니다. 이동 규칙은 `lib/dropdown.ts` 의 순수 함수이고, 커서는 `aria-activedescendant` 로 알립니다 |
| `3b1d129` | Tiptap | 본문 입력칸 4곳을 편집기 하나로 바꾸고 본문을 HTML 로 저장합니다. 첨부는 등록 전에도 등록 후와 같은 줄 모양입니다 |

**본문이 HTML 이 되면서 남이 쓴 글의 `script` 가 읽는 사람의 브라우저에서 실행될 수 있게
되었습니다.** `lib/richText.ts` 의 `sanitizeBody` 가 화면에 넣기 직전에 허용 목록에 없는 태그와
속성을 제거합니다(`DOMPurify`). 서버는 받은 HTML 을 그대로 저장하므로, 화면 말고 다른 곳에서
본문을 내보내게 되면 그쪽에서도 같은 처리를 해야 합니다.

`vitest` 는 Node 에서 돌아 DOM 이 없어 `DOMPurify` 가 동작하지 않습니다. `richText.test.ts` 한
파일만 `// @vitest-environment jsdom` 으로 DOM 환경에서 실행합니다.

### 마지막 헤드매니저가 사라지는 경로와 관리자코드

권한 항목을 전부 가진 permission set 의 보유자가 0명이 되는 경로가 4개였습니다.

| 경로 | 누가 | 처리 |
|---|---|---|
| 추방 `DELETE /members/{id}` | 남 | 막았습니다 |
| 남의 permission set 회수 | 남 | 막았습니다 |
| 자기 permission set 회수 | 본인 | 막지 않았습니다 |
| 탈퇴 `DELETE /me` | 본인 | 막지 않았습니다 |

뒤의 둘을 막지 않은 이유는 **관리자코드로 복구할 수 있기 때문** 입니다. 코드를 아는 사람이
계정을 하나 만들면 권한을 다시 받고, 권한을 잃은 사람에게 부여할 수 있습니다.

그래서 "계정이 0개인 DB 의 첫 가입자가 자동으로 전부 받는" 규칙을 없애고 관리자코드로
바꿨습니다(`dd34090`). `.env` 의 `ADMIN_SIGNUP_CODE` 와 같은 값을 가입 화면의 칸에 넣으면
권한 항목을 전부 받고, 없거나 다르면 일반 멤버로 가입합니다. 비교는 `hmac.compare_digest` 로
합니다 — `==` 는 다른 글자를 만나면 멈춰서 응답 시간 차이로 앞자리를 맞힐 수 있습니다.

### 작성 페이지 분리와 본문 임베드

**목록 아래에 작성 폼을 이어붙이고 있었습니다.** 개발자님이 그 전에도 여러 번 지적하셨는데
`dev_history` 에도 `.cluedoc` 에도 기록이 없어 세션마다 같은 구조로 다시 만들어졌습니다.
이번에 메모리(`feedback-write-page-not-inline-form`)에 남겼습니다.

`/notices/new` 와 `/board/:teamId/new` 를 만들고 목록의 글쓰기를 그리로 가는 링크로 바꿨습니다
(`708b969`).

**작성 페이지를 열면 빈 글(초안)을 먼저 만듭니다.** 본문에 파일을 드래그해 넣으려면 글 번호가
있어야 하는데, 첨부 업로드가 `POST /posts/{id}/attachments` 라 글이 없으면 쓰는 중에 올릴 수
없기 때문입니다. `posts.published_at` 이 비어 있으면 초안이고, 목록과 남의 상세 조회에서 빠지며
제목·본문이 비어 있어도 됩니다(`3e94d4e`·`aa9b1e5`).

버려진 초안은 두 단계로 지웁니다. 화면이 나갈 때 `DELETE /posts/{id}` 를 보내고, 탭을 닫거나
연결이 끊겨 그 요청이 가지 못한 행은 하루 뒤 자동 배정 확인이 함께 지웁니다
(`jobs/auto_assign.py` 의 `sweep_stale_drafts`).

**본문 임베드는 공식 확장 3개로 했습니다**(`53964ae`). 직접 만들지 않았습니다.

| 하는 일 | 쓴 것 |
|---|---|
| 떨구기·붙여넣기로 파일 잡기 | `@tiptap/extension-file-handler` |
| 소리 재생기 | `@tiptap/extension-audio` |
| 유튜브 임베드 | `@tiptap/extension-youtube` |
| 그림 | `@tiptap/extension-image`(이미 있던 것) |
| PDF 뷰어 | 오픈소스 노드가 없어 `<iframe>` 노드 하나를 만들었습니다. 뷰어는 브라우저에 내장돼 있어 라이브러리를 더하지 않았습니다 |

떨군 파일은 초안 번호로 올리고, 받은 주소를 확장자에 따라 그림·재생기·PDF 뷰어로 넣습니다
(`lib/richText.ts` 의 `embedKind`). 브라우저가 못 여는 형식(heic·flac·avi·mkv·zip 등)은 본문에
넣지 않고 첨부 목록에만 둡니다 — 깨진 그림이 뜨는 것보다 이름 줄이 낫습니다.

같은 작업에서 제가 만들어 둔 결함 2개를 고쳤습니다.

- `Link` 를 StarterKit 과 별도로 등록해 같은 이름이 둘이었습니다. 하나가 무시되어 `target`·`rel`
  설정이 적용되지 않을 수 있었습니다. StarterKit 의 설정으로 옮겼습니다
- 허용 태그에 구분선(`<hr>`)과 제목 4~6단계가 없어, 본문에 넣으면 저장은 되고 화면에서만
  사라졌습니다. 쓴 사람은 왜 없어졌는지 알 수 없습니다

`iframe` 은 태그만 허용하면 어떤 주소든 열리므로, 주소를 직접 확인해 첨부(`/`)와 유튜브 임베드가
아니면 태그째 지웁니다. 확인은 `richText.test.ts` 의 5개가 합니다.

## 사용자가 정한 것

- 남은 작업 7건을 한 세션에 전부 처리합니다. 작업마다 커밋합니다
- 마지막 헤드매니저가 자기 권한을 빼는 것과 탈퇴하는 것은 막지 않습니다. 관리자코드로 복구됩니다
- 관리자코드는 개발자가 정해 사람에게 직접 전달하는 값입니다. 길이 하한이나 무작위 생성을
  강제하지 않습니다 — 불러주고 받아적을 수 없는 값이면 쓰이지 않습니다
- 틀린 코드로 가입을 거절하지 않습니다. 일반 멤버로 가입시킵니다
- 관리자코드 칸은 가입 화면에 그대로 둡니다. 숨기지 않습니다
- 첫 가입자 자동 부여는 없앱니다. 배포 직후 개발자보다 먼저 가입한 사람이 전부 받는 경로였습니다
- **목록 아래에 작성 폼을 이어붙이지 않습니다.** 작성 페이지를 따로 만듭니다
- 버려진 초안은 하루 지난 것을 자동으로 지웁니다. 자동 배정 확인에 얹습니다
- 본문 임베드는 직접 만들지 않고 오픈소스를 찾아 씁니다
- 본문에 넣을 수 있는 것은 그림·소리·PDF·유튜브입니다
- 첨부 목록은 지금 모양(이름과 크기) 그대로 둡니다. 이름을 누르면 받아지므로 다운로드 아이콘을
  따로 두지 않습니다. 본문에 넣은 파일이 목록에도 남는 것도 그대로 둡니다
- QA 는 개발자님이 직접 합니다
- 블라인드 대상은 글입니다. 댓글은 글에 딸린 하위 객체라 따로 가리지 않습니다
- 해제는 권한자만 합니다. 권한 항목은 `board_moderate` 를 그대로 씁니다
- 누가 가렸는지를 남깁니다. 리뷰 지적을 받고 같은 세션에서 반영하기로 했습니다
- 권한자에게는 전부 보입니다. 자기가 속하지 않은 팀의 글도 처리하고 본문도 읽습니다

## 트러블슈팅

- **커밋 훅이 메시지를 거절:** PowerShell here-string(`@'...'@`)을 Bash 도구로 실행해 첫 줄이 빈 줄이
  되었습니다. 메시지를 파일에 쓰고 `git commit -F` 로 전달해 해결했습니다. 도구마다 문법이 다릅니다
- **`DOMPurify` 가 `sanitize is not a function` 으로 실패:** vitest 가 Node 에서 돌아 DOM 이 없어서였습니다.
  `happy-dom` 을 먼저 넣었더니 이번에는 `<p>` 까지 전부 제거해 동작이 달랐습니다. `jsdom` 으로 바꿔
  해결하고 `happy-dom` 은 지웠습니다
- **E2E 를 파일 하나만 실행하면 `global-setup` 이 실패:** 전용 DB 가 비어 있지 않아서입니다.
  `COMMAND.md` 12-1 의 첫 줄(`--force-recreate --wait e2e-api`)부터 실행해야 합니다
- **용어를 임의의 한글로 치환:** `sanitize` 를 "정화" 로 바꿔 쓰고 원래 용어를 남기지 않아
  개발자님이 무슨 뜻인지 되물으셨습니다. 8곳을 원래 용어로 되돌리고 뜻을 한 줄 덧붙였습니다.
  리베이스로 원래 커밋 3개에 넣었습니다
- **사람이 쓸 값에 기계 기준을 적용:** 관리자코드에 24자 하한과 `openssl rand` 안내를 넣었습니다.
  사람에게 불러주는 값이라 쓸 수 없는 값이 됩니다. 하한과 기록을 전부 지우고 비교만 남겼습니다

## 남은 것

### 1. 기능

없습니다. 2026-09-15 에 정한 목록과 그 뒤에 나온 항목까지 전부 처리했습니다.

**개발자님이 하실 일** — `.env` 에 `ADMIN_SIGNUP_CODE` 를 적고 서버를 다시 시작합니다.
적지 않으면 새로 세우는 저장소에서 관리자 계정을 만들 수 없습니다.

### 2. 끝나지 않은 것

- 모달 폼의 날짜는 표시만 합니다
- 화면에서 손으로 눌러 보지 않은 것 — 본문에 넣은 소리·PDF·유튜브, 점유 단위 변경, Tiptap
  도구 막대(굵게·기울임·취소줄·글꼴·링크), 드롭다운 14곳 중 대부분. E2E 가 지나가는 것은
  본문 그림 넣기와 배정 화면의 기간 고르기입니다

### 3. 알고 넘긴 것

`dev_history/2026-09-15.md` 의 9건이 그대로이고, 오늘 13건이 추가되었습니다.

- 블라인드에 "왜" 가렸는지는 남기지 않습니다. 누가 가렸는지만 남습니다. 사유가 필요해지면 열을 하나 더 둡니다
- 권한자는 가려진 글의 첨부를 삭제할 수는 있지만 받을 수는 없습니다. 가려진 글의 상세를 열 수 없어
  화면에서는 닿지 않는 경로입니다. 리뷰에서 LOW 로 지적받았고 그대로 두었습니다
- 자동 배정이 5분 상한을 넘겨 실패하면 실행 기록을 남기지 않아 다음 확인에서 다시 실행합니다.
  상한을 넘기는 입력은 확인마다 5분을 다시 소모합니다. 이번 결정 범위 밖이라 변경하지 않았습니다
- 링크 미리보기 이미지가 `/images/in_six_strings.jpg` 입니다. 미리보기 전용 이미지가 아니라 가로세로
  비율이 맞지 않을 수 있습니다. 전용 이미지를 만들면 `frontend/index.html` 의 `og:image` 한 줄을 바꿉니다
- 미리보기는 사이트 1개 단위입니다. 게시글 주소를 붙여넣어도 그 글의 제목이 아니라 서비스 제목이
  나옵니다. 글마다 다르게 하려면 크롤러 요청만 서버로 넘겨 태그를 만들어 붙여야 합니다
- `COMMAND.md` 10-4 의 주의점에 "이 bundle 을 실제로 제공하는 서버는 아직 없습니다" 가 남아 있습니다.
  지금은 nginx 가 제공합니다. 이번 작업 범위가 아니라 고치지 않았습니다
- 배정 구간 합치기 전에 저장된 행은 합치지 않았습니다. 다음 계산이 그 기간의 행을 전부 다시 쓰므로
  그때 합쳐집니다. 그때까지 칸마다 한 행으로 남아도 서로 겹치지 않아 새 제약에 걸리지 않습니다
- 본문 HTML 의 `sanitize` 는 화면에서만 합니다. 서버는 받은 값을 그대로 저장합니다
  (`lib/richText.ts` 의 `ponytail:` 주석)
- 관리자코드를 추측하는 시도를 막는 것은 가입 요청 제한(1시간 5회) 하나이고, 이 제한은 발신 주소
  단위라 주소를 바꾸면 우회됩니다. security-reviewer 가 HIGH 로 지적했고, 발신 주소와 무관한 상한을
  두는 방안은 공격자가 소진시키면 개발자님도 관리자 가입을 못 하게 되어 두지 않았습니다
- 드롭다운 선택지의 누르기 처리에 `jsx-a11y/click-events-have-key-events` 를 1줄 끕니다. 초점과 키
  입력이 버튼에 있고 커서는 `aria-activedescendant` 로 알리는 구조라, 선택지 자체에는 키 처리가 없습니다
- 본문에 넣은 파일이 첨부 목록에도 남습니다. 같은 파일이 두 군데 보입니다. 목록에서 빼면 받을
  방법이 없어져 그대로 두기로 했습니다(사용자 결정)
- 작성 페이지를 열 때마다 초안 행이 하나 생깁니다. 글을 쓰지 않고 나가면 지우지만, React 의
  StrictMode 가 개발 중에 effect 를 두 번 실행해 초안이 하나 더 생길 수 있습니다. 그 행은 하루 뒤
  sweep_stale_drafts 가 지웁니다
- 첨부 업로드 진행률을 본문 떨구기에서는 표시하지 않습니다. 첨부 입력칸으로 올릴 때만 표시합니다
