# 화면 텍스트 목록

`frontend/src` 내의 모든 텍스트입니다. 테스트 파일(`*.test.ts`)은 제외했습니다.

**기능** 열은 코드를 읽고 작성했습니다 — **표시되는 시점(조건)**과 **선택했을 때 발생하는 동작(결과)**.

**노출** — `화면`: 시각적으로 표시됨 / `보조`: 스크린 리더만 읽음 / `창`: 브라우저 confirm / `내부`: throw된 후 처리되는 문구



## 15. lib — 검사·표시 문구

### 권한 항목 열여덟 개 (`account.ts:17-107`)


### 역할 (`account.ts:6`)

| 텍스트 | 기능 |
|---|---|
| 헤드매니저 | 18개 항목을 모두 보유한 사람입니다. 서버가 판정하여 전달합니다 |
| 일반 멤버 | 그 외 모든 사용자 |

