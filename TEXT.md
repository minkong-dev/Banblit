# 화면 텍스트 목록

`frontend/src` 안의 모든 텍스트. 테스트 파일(`*.test.ts`)은 뺐다.

**기능** 칸은 코드를 읽고 적었다 — **언제 뜨는지(조건)** 와 **누르면 무슨 일이 일어나는지(동작)**.

**노출** — `화면` 눈에 보임 / `보조` 화면 낭독기만 읽음 / `창` 브라우저 confirm / `내부` throw 된 뒤 잡히는 문구

---

---

## 11. SettingsMembers.tsx — 설정 · 멤버 

### 권한 주기 창

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 179 | (권한 이름) / 이 권한을 가진 사람 | 화면 | 창 제목·부제 |
| 182 | 아직 이 권한을 가진 사람이 없습니다 | 화면 | 아무도 없을 때 |
| 193 | ○○ 에게서 권한 빼기 | 보조 | 쓰레기통 |
| 205 | 멤버 추가 | 화면 | 사람 찾기. 이미 가진 사람은 결과에서 빠진다 |
| 165 | 권한을 주었습니다 | 화면 | 성공 토스트 |
| 172 | 권한을 뺐습니다 | 화면 | 성공 토스트 |

### 멤버 표

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 374-375 | 멤버 / 가입한 사람 전부입니다 | 화면 | 카드 머리글 |
| 378 | 권한으로 거르기 | 보조 | 거르개 |
| 382 | 전체 | 화면 | 거르개 기본값 |
| — | (권한 이름들) | 화면 | **지금까지 받아온 줄에서 뽑은 이름만** 목록에 오른다 |
| 393-397 | 이름 / 학과 / 학번 / 기수 / 권한 | 화면 | 표 머리글 |
| 409 | — | 화면 | 학과·학번·기수·권한이 비었을 때 |
| 416 | 보여줄 사람이 없습니다 | 화면 | 거른 결과가 0명 |
| 418 | 더 불러오는 중… | 화면 | **이 줄이 화면에 들어오면 다음 50명을 부른다.** 다 받으면 빈 문자열이 된다 |

---

## 12. SettingsAccount.tsx — 설정 · 계정 탭

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 212 | 불러오는 중… | 화면 | `/me` 가 아직 안 왔을 때 카드 대신 |

### 내 정보

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 44-45 | 내 정보 / 이름과 기수를 고칩니다. 이메일은 바꿀 수 없습니다 | 화면 | 카드 머리글 |
| 49 | 이름 | 화면 | 라벨 |
| 53, 57 | 기수 / 예: 46 | 화면 | 라벨·placeholder. **비우면 기수 없음으로 저장된다** |
| 69 | 이름을 입력해 주세요. | 화면 | 이름이 공백뿐일 때. 그 밖의 판정은 서버가 한다 |
| 74 | 저장하는 중… / 저장 | 화면 | 보내는 버튼 |
| 36 | 내 정보를 고쳤어요. | 화면 | 성공 토스트 |

### 비밀번호

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 108-109 | 비밀번호 / 지금 비밀번호를 확인한 뒤 새 것으로 바꿉니다 | 화면 | 카드 머리글 |
| 113, 123, 133 | 지금 비밀번호 / 새 비밀번호 / 새 비밀번호 확인 | 화면 | 입력칸 셋 |
| 150 | 새 비밀번호가 서로 다릅니다. | 화면 | **화면이 보는 것은 이것 하나뿐이다.** 길이·대소문자·기호 규칙은 서버가 판정하고 그 사유가 여기 뜬다 |
| 155 | 바꾸는 중… / 비밀번호 바꾸기 | 화면 | 보내는 버튼 |
| 100 | 비밀번호를 바꿨어요. | 화면 | 성공 토스트. 세 칸을 비운다 |

### 화면 밝기

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 76-77 | 화면 / 이 브라우저에만 남습니다 | 화면 | 카드 머리글 |
| 80 | 화면 밝기 | 보조 | 묶음 이름 |
| 69-70 | 밝게 / 어둡게 | 화면 | 누르면 곧바로 바뀌고 브라우저에 남는다. **저장이 막힌 브라우저에서도 화면은 바뀐다**(이번만) |

처음 값은 저장된 것, 없으면 운영체제 설정을 따른다.

### 회원 탈퇴

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 178-179 | 회원 탈퇴 / 계정과 함께 내가 쓴 글·댓글·예약이 모두 사라집니다. 되돌릴 수 없습니다 | 화면 | 카드 머리글 |
| 183 | 확인을 위해 내 이름을 적어 주세요 | 화면 | 라벨. placeholder 로 내 이름이 흐리게 떠 있다 |
| 198 | 탈퇴하는 중… / 탈퇴하기 | 화면 | **적은 이름이 내 이름과 정확히 같을 때만 눌린다.** 되묻는 창은 없다 |
| — | (없음) | — | 성공하면 `/` 로 간다. **성공 알림이 없다** |

---

## 13. Assignment.tsx — `/admin` 배정 결과 확인

`assign_read` 가 있어야 사이드바에 뜬다. 집중 합주기간만 다룬다.

### 탭

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 487 | 배정안 | 보조 | 탭 묶음 이름 |
| 231 | 현재 확정된 배정안 | 화면 | 확정 시간표를 달력에 그린다 |
| 234 | A안, B안, … | 화면 | **다시 계산한 결과 자리를 다 못 채웠을 때만** 생긴다. 제안 수만큼 |
| 236 | (9월 7일 18:03) | 화면 | 오른쪽에서 지난 회차를 골랐을 때만 생기는 탭 |

### 달력 위

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 492 | 집중 합주기간 | 화면 | 머리글 |
| 494 | (첫날 – 마지막날) / 일정이 없어요 | 화면 | 지금 보고 있는 것의 날짜 범위 |
| 496 | 기간 선택 | 보조 | 고르개. **번호가 아니라 날짜 범위로 보여준다.** 바꾸면 탭이 "현재 확정된 배정안"으로 돌아간다 |
| 518 | 캘린더에 표시할 일정이 없어요 | 화면 | 그릴 것이 하나도 없을 때 |
| 543 | 합주 없음 | 화면 | 그 날짜칸에 아무것도 없을 때 |

### 달력 아래 — 상태에 따라 통째로 갈린다

**(1) 조회 실패**

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 293 | 서버가 요청을 받지 못했습니다 | 화면 | 기간·합주실·팀·시간표·재계산 중 하나라도 실패 |
| 294 | (사유) / 알 수 없는 오류 | 화면 | 첫 실패 사유 |

**(2) 지난 회차를 고름**

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 302 | ○○에 밀려난 시간표입니다 | 화면 | 제목 |
| 305-308 | 불러오는 중… / 이 회차를 불러오지 못했습니다 / 합주 n번 · 모두 합쳐 n시간. 지나간 회차라 보기만 합니다. | 화면 | 조회 상태 |
| 254 | (팀) 합주 n번 · n시간 | 화면 | 팀별 요약. 그 회차에 없는 팀은 흐리게 |
| 312 | 지금 것으로 돌아가기 | 화면 | 확정 배정으로 |

**(3) 배정 제안을 고름**

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 320 | ○○ 멤버를 제외하면 될 것 같아요. | 화면 | 그 제안에서 빠지는 사람 이름 |
| 322 | 해당 인원의 불가능 시간으로 인해 이상적인 배정안을 찾지 못했어요. | 화면 | 사유 |
| 326-327 | 현재 캘린더는 해당 멤버를 제외한 예상 결과를 보여주고 있어요. 확정할 경우에만 해당 배정안으로 확정되니, 신중하게 결정해주세요. | 화면 | 안내 |
| 333 | 배정안 확정 중… / 해당 배정안으로 확정 | 화면 | **`proposal_confirm` 이 있어야 보인다.** 그 사람을 뺀 채로 다시 계산해 저장한다 |
| 127 | 선택한 배정으로 확정했어요 / 해당 배정을 선택하지 않았어요 | 화면 | 확정 결과 토스트 |
| 129 | 해당 배정을 선택하는데 실패했어요 | 화면 | 확정 실패 |
| 336 | 현재 배정안으로 돌아가기 | 화면 | 확정 배정으로 |

**(4) 확정된 것이 없음**

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 343 | 현재 확정된 배정안이 없어요 | 화면 | 확정 시간표가 비었을 때 |
| 345-346 | 현재 팀이나 멤버가 없거나, 집중 합주기간이 아니에요. 생성된 팀과 가입한 멤버가 있는지 확인해주세요. | 화면 | 사유 |
| 353-354 | 아래의 **스케줄링** 버튼을 누르면 스케줄링 엔진이 즉시 다시 연산을 시작해요. 즉시 연산이 필요할 경우에만 사용해주세요. | 화면 | **`assign_run` 이 있는 사람에게만.** 가리키는 버튼의 실제 이름은 `지금 다시 계산` 이다 |
| 357 | 관리자가 배정을 진행할 경우 배정안이 여기에 표시돼요. | 화면 | `assign_run` 이 없는 사람에게 |

**(5) 확정된 것이 있음**

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 367 | 확정된 배정안이에요 | 화면 | 제목 |
| 369 | 합주 n번이고, 총 n.n시간이에요. | 화면 | 요약 |
| 373 | 다시 스케줄링하면 현재 배정안은 기록 후 새 배정안으로 변경되어요. | 화면 | `assign_run` 이 있는 사람에게만 |

### 버튼 줄 (권한 둘 다 없으면 줄 자체가 없다)

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 270 | 계산하는 중… / 지금 다시 계산 | 화면 | **`assign_run`.** 확정 시간표에 나온 팀·방 번호를 그대로 넘겨 다시 계산한다. 아직 확정된 것이 없으면 전체 팀·방을 넘긴다 |
| 54 | 모든 시간을 다 사용하지는 못했어요 · 배정 제안 n건 | 화면 | 자리를 다 못 채웠을 때. 위에 A안·B안 탭이 생긴다 |
| 56 | 선택한 배정으로 확정했어요 / 해당 배정을 선택하지 않았어요 | 화면 | 다 채웠을 때. 저장까지 됐는지로 갈린다 |
| 111 | 엔진이 연산에 실패했어요 | 화면 | 계산 요청·대기가 걸렸을 때. **서버가 준 사유를 버리고 이 문구만 띄운다** |
| 282 | 되돌리는 중… / 이전 배정안으로 되돌리기 | 화면 | **`rollback`.** 언제나 **바로 직전 회차 하나**로만 간다 — 오른쪽에서 고른 회차로 가지 않는다 |
| 278 | 현재 확정된 시간표를 해당 배정안으로 되돌려요. 해당 작업은 진행 후 다시 복구하기 어려워요. 그래도 되돌릴까요? | 창 | 되돌리기 전 되묻는다 |
| 145 | 이전 배정안으로 되돌렸어요 / 이전 배정기록이 없어요 | 화면 | 되돌리기 결과 |
| 148 | 이전 배정기록으로 되돌리는데 실패했어요 | 화면 | 서버가 거절. 사유가 있으면 사유가 대신 뜬다 |

### 오른쪽 — 이전 배정기록

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 411 | 이전 배정기록 / 캘린더에서 미리보기가 가능해요 | 화면 | 칸 머리글 |
| 386 | 이전 배정 되돌리기 권한이 있어야 확인이 가능해요. | 화면 | **`rollback` 이 없으면 목록을 부르지도 않는다** |
| 388, 390, 392 | 이전 배정기록 불러오는 중… / 이전 배정기록을 불러오지 못했어요. / 이전 배정기록이 없어요. | 화면 | 조회 상태 |
| 404 | (9월 7일 18:03) | 화면 | 회차 저장 시각. **최근 것이 맨 위** |
| 405 | n칸 / 빈 회차 | 화면 | 그 회차가 들고 있던 칸 수 |
| 405 | · 되돌린 배정안은 여기로 | 화면 | **맨 위 회차에만** 붙는다 — 되돌리기가 가는 곳이 거기라는 표시 |

### 오른쪽 — 스케줄링 시간

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 429 | 스케줄링 시간 | 화면 | 항목 이름 |
| 432 | 집중 합주기간을 생성해주세요 | 화면 | 고른 기간이 없을 때. 입력칸도 안 나온다 |
| 433 | ○○ · ○○ | 화면 | 지금 저장된 두 시각 |
| 442, 452 | 1차 스케줄링 시간 / 2차 스케줄링 시간 | 보조 | **`step=1800` — 30분 단위.** 같은 값을 설정 화면에서는 1분 단위로 고른다. `period_edit` 이 없으면 잠긴다 |
| 468 | 변경사항 저장 중… / 변경사항 저장 | 화면 | **`period_edit` 이 있어야 보이고, 값이 바뀌고 검사를 통과해야 눌린다** |
| — | 계산 시각을 두 개 다 정해 주세요 | 화면 | 둘 중 하나가 비었을 때 (`runs.ts:13`) |
| — | 두 계산 시각은 서로 달라야 합니다 | 화면 | 두 값이 같을 때 (`runs.ts:15`) |
| 473 | 지정한 시간에 스케줄링을 진행해요. | 화면 | 검사 통과 + `period_edit` 있음 |
| 474 | 집중 합주기간이 있어야 시간을 지정할 수 있어요. | 화면 | 검사 통과 + `period_edit` 없음. **기간은 이미 있는 상태라 문구가 상황과 안 맞는다** |
| 188 | 엔진 연산 시간을 저장했어요 | 화면 | 성공 토스트 |
| 190 | 엔진 연산 시간을 저장하는데 실패했어요. | 화면 | 실패 토스트 |

### 내부 문구

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 88, 99, 118, 134, 157, 178 | 선택할 집중 합주기간이 없어요 | 내부 | 기간이 없는데 조회·전송이 불린 경우. **여섯 곳에 같은 문구가 복사돼 있다.** 조회는 기간이 없으면 아예 안 도므로 실제로는 거의 안 뜬다 |
| 168 | 선택할 이전 배정기록이 없어요 | 내부 | 위와 같음 |

---

## 14. 공용 부품

### MemberSearch.tsx — 사람 찾기

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 205-206 | 검색어 / 이름을 입력해주세요 | 보조·화면 | 입력칸. 열리면 곧바로 초점이 온다 |
| 178 | 검색을 위해 이름을 입력해주세요. | 화면 | 빈 검색어. **서버를 부르지 않는다** |
| 180 | 검색 중… | 화면 | 조회 중 |
| 182 | (사유) | 화면 | 조회 실패 |
| 184 | 해당하는 사용자를 찾지 못했어요. | 화면 | 결과가 없거나, 나온 사람이 전부 이미 고른 사람일 때 |
| 191 | 이름 (46기) | 화면 | 결과 줄. 누르면 고른다 |

### Modal.tsx

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 246 | 닫기 | 보조 | X 버튼. Esc 와 바깥은 브라우저 `<dialog>` 가 맡는다 |
| 273, 283 | ○○ 하나 줄이기 / ○○ 하나 늘리기 | 보조 | −/+ 버튼. 한계에 닿으면 잠긴다 |

### Field.tsx

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 345 | 비밀번호 가리기 / 비밀번호 보기 | 보조 | **비밀번호 칸에만 붙는다.** 누르면 글자가 그대로 보인다 |

### Pager.tsx

| 줄 | 텍스트 | 노출 | 기능 |
|---|---|---|---|
| 363 | 쪽 넘기기 | 보조 | 묶음 이름 |
| 364, 375 | 이전 쪽 / 다음 쪽 | 보조 | 첫 쪽·끝 쪽에서 잠긴다 |
| 367 | n쪽 | 보조 | **번호는 다섯 개까지만 보이고 지금 쪽이 가운데 온다** |

---

## 15. lib — 검사·표시 문구

### 권한 항목 열여덟 개 (`account.ts:17-107`)

권한 만들기 창의 스위치 목록. **왼쪽이 실제 키다.**

| 키 | 이름 | 설명 | 화면에서 실제로 쓰는 곳 |
|---|---|---|---|
| `room_create` | 합주실 만들기 | 합주실을 새로 만듭니다. | 설정 합주실 탭·+ 새 합주실 |
| `room_edit` | 합주실 수정 | 합주실 이름과 여닫는 시각을 고칩니다. | 설정 합주실 탭·연필 |
| `period_create` | 기간 만들기 | 상시 개방기간과 집중 합주기간을 새로 만듭니다. | 설정 기간 탭·+ 새 기간 |
| `period_edit` | 기간 수정 | 기간의 날짜와 계산이 도는 시각을 고칩니다. | 설정 기간 탭·연필, 배정 화면 스케줄링 시간 |
| `team_create` | 팀 만들기 | 팀을 새로 만들고 포지션 구성을 정합니다. | + 새 팀 |
| `team_edit` | 팀 수정 | 팀 이름을 바꿉니다. | 팀 줄 연필 |
| `team_delete` | 팀 삭제 | 팀을 지웁니다. 그 팀의 포지션도 함께 사라집니다. | 팀 줄 쓰레기통 |
| `member_add` | 포지션에 사람 추가 | 팀의 빈 포지션에 사람을 넣습니다. | 구성 창 돋보기 |
| `member_remove` | 남을 포지션에서 빼기 | 다른 사람이 있는 포지션을 비웁니다. 자기 포지션은 이 권한 없이도 뺄 수 있습니다. | 구성 창 삭제. **설명과 달리 자기 자리도 함께 감춰진다** |
| `notice_write` | 공지 쓰기 | 모두가 보는 공지사항에 글을 올립니다. | 공지 글쓰기 버튼 |
| `board_moderate` | 남의 글·댓글 다루기 | 다른 사람이 쓴 글과 댓글을 고치거나 지웁니다. | **화면에서 아무 데도 안 쓴다** |
| `reservation_manage` | 남의 예약 다루기 | 다른 사람이 잡은 예약을 옮기거나 취소합니다. | **화면에서 아무 데도 안 쓴다** |
| `assign_run` | 배정 계산 실행 | 기간의 시간표를 지금 다시 계산합니다. | 지금 다시 계산 |
| `assign_read` | 배정 결과 보기 | 아직 확정되지 않은 조율안과 거기서 빠지는 사람의 이름을 봅니다. | 사이드바 배정 결과 확인 |
| `proposal_confirm` | 조율안 확정 | 조율안 하나를 골라 시간표로 확정합니다. | 해당 배정안으로 확정 |
| `rollback` | 되돌리기 | 지난 계산 회차 목록을 보고, 시간표를 직전 회차로 되돌립니다. | 되돌리기 버튼, 이전 배정기록 칸 |
| `permission_manage` | 권한 만들기·수정·삭제 | 권한 묶음 자체를 만들고 고치고 지웁니다. | 설정 멤버 탭이 열리는 조건 |
| `permission_grant` | 권한 주고 뺏기 | 만들어 둔 권한을 사람에게 주거나 뺍니다. | 설정 멤버 탭이 열리는 조건 |

### 역할 (`account.ts:6`)

| 텍스트 | 기능 |
|---|---|
| 헤드매니저 | 열여덟 항목을 전부 가진 사람. 서버가 판정해 내려준다 |
| 일반멤버 | 그 밖 전부 |

### api.ts — 서버 호출

| 줄 | 텍스트 | 기능 |
|---|---|---|
| 45 | 서버가 n초 안에 답하지 않아 끊었습니다 | 요청 제한 시간을 넘김 |
| 46, 103 | 서버에 닿지 못했습니다 | 연결 자체가 실패 |
| 105 | 파일을 n분 안에 다 올리지 못해 끊었습니다 | 업로드 제한 시간을 넘김 |
| 121 | 알 수 없는 오류가 났습니다 | `Error` 가 아닌 것이 던져졌을 때의 기본값 |

서버가 거절하면 응답의 `detail` 을 그대로 문구로 쓴다.

### jobs.ts — 배정 계산 대기

| 줄 | 텍스트 | 기능 |
|---|---|---|
| 398 | 계산 결과가 비어 있습니다 | 끝났다는데 결과가 없을 때 |
| 402 | 계산하지 못했습니다 | 서버가 실패로 끝냈는데 사유를 안 줬을 때 |
| 405 | 계산이 60초 안에 끝나지 않았습니다 | 0.7초마다 되묻다 60초를 넘김 |

**이 셋은 화면에 안 보인다** — 배정 화면이 사유를 버리고 `엔진이 연산에 실패했어요` 만 띄운다.

### notifications.ts — 알림 문구

| 줄 | 텍스트 | 기능 |
|---|---|---|
| 259 | 합주 시간표가 새로 짜였습니다 | `assignment_updated` |
| 265 | 새 소식이 있습니다 | **모르는 종류가 왔을 때.** 서버가 새 종류를 늘리면 이 문구가 뜬다 |

### validate.ts / settings.ts / roster.ts / boards.ts — 검사 문구

| 파일·줄 | 텍스트 | 언제 |
|---|---|---|
| validate 85 | 이메일을 입력해 주세요. | 빈칸 |
| validate 86 | 이메일 형식이 맞는지 확인해주세요. | `@` 앞뒤에 공백 없는 글자, 점 뒤 두 글자 이상이 아닐 때 |
| validate 92 | 비밀번호를 입력해 주세요. | 로그인·가입 빈칸 |
| validate 93 | 비밀번호는 8자 이상으로 작성해주세요. | **로그인 칸에만.** 규칙은 안 본다 |
| validate 101 | 8자에서 20자 사이로 입력해주세요. | 가입·재설정 |
| validate 103-106 | 소문자를 / 대문자를 / 숫자를 / 특수기호를 하나 이상 넣어주세요. | 가입·재설정. **하나씩 차례로 알린다** |
| validate 118 | 새 비밀번호를 입력해주세요. | 재설정 빈칸 |
| validate 126 | 기수를 입력해 주세요. | 빈칸 |
| validate 129 | 기수는 1에서 200 사이의 숫자여야 해요. | 정수 아님·범위 밖 |
| validate 138 | ○○ 이름을 입력해 주세요. | 합주실·팀 이름 빈칸 |
| validate 140 | 같은 이름의 ○○이 이미 있습니다. | 앞뒤 공백을 뗀 뒤 견준다 |
| settings 165-166 | 여는 시각을 / 닫는 시각을 입력해 주세요. | 빈칸 |
| settings 170-171 | 여는 시각은 / 닫는 시각은 정시여야 합니다. | 분이 00이 아님 |
| settings 172 | 닫는 시각은 여는 시각보다 늦어야 합니다. | 순서 오류 |
| settings 181-182 | 시작하는 날을 / 끝나는 날을 골라 주세요. | 빈칸 |
| settings 185 | 끝나는 날은 시작하는 날보다 빠를 수 없습니다. | 글자 그대로 견준다 |
| roster 449 | 악기를 하나 이상 골라 주세요. | 팀 자리 합계 0 |
| roster 451 | 한 팀의 자리는 10개까지입니다. | 합계 11 이상. **버튼가 먼저 잠기므로 보통 안 뜬다** |
| boards 9-10 | 제목을 입력해 주세요. / 제목은 200자를 넘을 수 없습니다. | 글 제목 |
| boards 14-15 | 내용을 입력해 주세요. / 내용은 20000자를 넘을 수 없습니다. | 글 내용 |
| boards 19-20 | 댓글을 입력해 주세요. / 댓글은 2000자를 넘을 수 없습니다. | 댓글 |
| boards 67 | 올릴 수 없는 형식입니다. ○○만 올릴 수 있습니다. | 확장자가 허용 목록에 없음 |
| boards 70-71 | 파일 하나는 300MB까지 올릴 수 있습니다. 이 파일은 ○○입니다. | 크기 초과 |

첨부 허용 확장자는 34가지인데 **안내에는 흔한 것만 적는다** — `doc`, `hwpx`, `xls`, `csv`, `heic`, `flac` 등은 올라가지만 안내 문구에 없다.

### confirm.ts — 되묻는 창

| 줄 | 텍스트 | 기능 |
|---|---|---|
| 425 | ○○을(를) 삭제할까요? | 받침이 있으면 `을`, 없으면 `를`. 한글이 아니면 `를` |
| 431 | ○○을(를) 취소할까요? | **예약에만 쓴다** — 버튼·물음·알림이 같은 말을 쓰게 |

### loading.ts / calendar.ts / roster.ts / runs.ts — 표시 형식

| 줄 | 텍스트 | 기능 |
|---|---|---|
| loading 241 | 불러오는 중… | 목록 공용 대기 문구 |
| calendar 32 | 18:00 | 칸 번호를 시각으로 |
| calendar 43 | n시간 | 칸 개수를 시간으로 |
| calendar 148 | 9월 13일 | 날짜 |
| calendar 154 | 9월 13일 일요일 | 날짜 + 요일 |
| calendar 161 | 9월 4일 14:30 | 날짜 + 시각. **시각은 적힌 글자를 그대로** 쓴다 |
| calendar 172 | 일 월 화 수 목 금 토 | 달력 머리글 |
| roster 460 | 드럼 / 드럼 2 | 같은 악기가 하나뿐이면 번호를 안 붙인다 |
| roster 465 | 이름 (46기) | 기수가 없으면 이름만 |
| runs 7 | n칸 / 빈 회차 | 회차가 든 칸 수 |
| boards 59 | 300MB | 바이트를 사람이 읽는 크기로 |

### contract.ts — 악기 여섯

`보컬`, `일렉`, `통기타`, `베이스`, `신디`, `드럼` — 팀 만들기·수정의 −/+ 줄 순서가 이 순서다.

### main.tsx

| 줄 | 텍스트 | 기능 |
|---|---|---|
| 453 | index.html 에 #root 가 없습니다 | 앱을 붙일 자리를 못 찾음. 화면에 아무것도 안 뜬다 |

---

## 16. 코드를 읽고 확인된 것

### 문구가 코드와 안 맞는 곳

| 자리 | 내용 |
|---|---|
| `Assignment.tsx:474` | `집중 합주기간이 있어야 시간을 지정할 수 있어요.` 가 **기간이 있는데** 뜬다. 이 자리는 기간이 없으면 아예 안 그려지고, 실제 조건은 `period_edit` 권한이 없는 것이다 |
| `Assignment.tsx:353` | 안내는 **스케줄링** 버튼이라고 하는데 실제 버튼 이름은 `지금 다시 계산` |
| `account.ts:61` | `member_remove` 설명은 "자기 포지션은 이 권한 없이도 뺄 수 있습니다" 인데, `Teams.tsx:256` 은 이 권한이 없으면 자기 자리의 삭제 버튼도 감춘다 |
| `Landing.tsx:188` | "합주실 수가 늘어나도 괜찮아요" 인데 `Settings.tsx:356` 은 합주실이 하나라도 있으면 만들기 버튼를 감춘다 |
| `Landing.tsx:37` | `Blit AI에게 말해주세요` — 코드에 그런 입구가 없다 |
| `hooks.ts:551` | 주석은 프로필 말풍선의 팀 색이 "스케줄러와 같은 규칙"이라는데, 말풍선은 **전체 팀 목록** 순서로, 달력은 **확정 시간표에 나온 팀** 순서로 색을 매긴다. 같은 팀이 두 곳에서 다른 색으로 보일 수 있다 |

### 같은 값을 다른 단위로 고치는 곳

| 자리 | 단위 |
|---|---|
| `Settings.tsx:280,288` 첫 계산·두 번째 계산 | `step` 없음 — 1분 |
| `Assignment.tsx:441,451` 1·2차 스케줄링 시간 | `step=1800` — 30분 |

둘 다 같은 `first_run_at`·`second_run_at` 을 같은 endpoint 로 고친다.
합주실 여닫는 시각은 `step=3600`(정시)이고 검사도 정시만 통과시킨다.

### 만들 수는 있는데 화면이 안 보는 권한

`board_moderate`, `reservation_manage` 두 가지. 권한 창에서 켜고 사람에게 줄 수 있지만
화면 어디에서도 읽지 않는다 — 남의 글을 고치는 연필은 글쓴이에게만, 남의 예약을 지우는
쓰레기통은 예약한 사람에게만 뜬다.

### 사유를 버리는 곳

`Assignment.tsx:111` 재계산 실패는 `엔진이 연산에 실패했어요` 만 띄우고 서버·대기 쪽 사유를
버린다. `jobs.ts` 가 만드는 문구 셋(`계산 결과가 비어 있습니다`, `계산하지 못했습니다`,
`계산이 60초 안에 끝나지 않았습니다`)은 그래서 화면에 나오지 않는다.
같은 화면의 되돌리기·저장은 사유를 그대로 보여준다.

### 글자 문제

| 자리 | 내용 |
|---|---|
| `Landing.tsx:25` | `Calender Fix Engine` — `Calendar` 오타 |
| `Landing.tsx:109` | 메뉴 항목 이름이 `TITLE` — 자리표시자로 보인다 |
| `Assignment.tsx:190` | 이 문구에만 마침표가 붙어 있다 |
| `Assignment.tsx:345` | 문장 끝에 공백이 남아 있다 |
| `AppShell.tsx:361` | 주석 줄 끝에 공백이 남아 있다 |
| `Assignment.tsx:382` | 주석에 작업 중 메모 `< 이게 뭔말인데;` 가 남아 있다 |

### 말투가 갈리는 곳

- `-습니다` — 공지·게시판·설정 전부, 권한 항목 열여덟 개, `api.ts`·`validate.ts`·`boards.ts` 검사 문구
- `-어요` — 배정 화면, 계정 다섯 화면, 달력·날짜 창, 팀 화면 토스트

같은 화면 안에서도 갈린다. 팀 화면은 목록이 `아직 팀이 없습니다.` 인데 토스트는 `○○ 팀을 만들었어요.` 다.

### 옛 용어가 남은 곳

화면 문구는 `배정 제안`·`엔진 연산` 으로 바뀌었는데 아래는 `조율안`·`계산` 그대로다.

- `account.ts:85,90,95` — 권한 설명 세 개. **권한 만들기 창에 그대로 보인다**
- `runs.ts:13,15` — `계산 시각을 두 개 다 정해 주세요` / `두 계산 시각은 서로 달라야 합니다`. **배정 화면 오른쪽에 보인다**
- `Settings.tsx:60,279,287` — `계산 ○○`, `첫 계산`, `두 번째 계산`. **설정 기간 탭에 보인다**
- `jobs.ts:398,402,405` — 계산 실패 문구 셋 (화면에는 안 나온다)
- `Assignment.tsx:20,51,114,163` / `pipeline.ts:134` / `jobs.ts:16` — 주석
