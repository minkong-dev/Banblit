개선
밴블릿 링크시 표시할 임베드(프리뷰) 만들기

파일 첨부 모달 UI 개선 (내장 UI가 아닌 디자인 핏에 맞는 UI 만들기)

파일 첨부시 임베드 가능한 형식(이미지, 링크 등 프리뷰 가능하게)

임베드 프리뷰로 웬만한건 해결이 가능하게 하고, 다운로드가 필요한 사람은 프리뷰 하단 다운로드 아이콘 눌러서 다운받을 수 있도록 하기

게시글, 공지사항, 댓글 등 공유되는 글을 작성하는 부분의 텍스트 필드를 텍스트 편집툴 공용 컴포넌트로 배치할것.
폰트 변경, 취소줄, 볼드, 이탤릭, 이미지 첨부, 링크 첨부 등 오픈소스로 공유되는 공용 컴포넌트를 말하는 것

예약의 경우 10분 단위로 시작시간과 끝시간을 지정가능하게 할것

예약 및 불가능 일정을 생성할때 일정 명을 생성할 수 있게 할것
해당 일정명은 캘린더에 표시되는 이름이 되며, 미지정시 기본값인 "불가능 일정" 으로 표시될것.
모달을 표시했을때, 여러 일정을 등록가능하도록 지원할 것.
모달 개선 UI예시인 저장소 루트의 모달개선예시.png를 참고할것.


집중합주기간을 팀별합주기간과 전체합주기간으로 분리할 것
팀별합주기간이 현재의 집중합주기간 배정의 성격을 띰.
전체합주기간은 옵셔널하게 선택 가능하게 할것
전체합주기간을 옵션을 설정했을 경우, 팀별합주기간과 전체합주기간을 설정하는 세부옵션을 표시하고, 전체합주기간의 기본값이 될 합주시작시간을 선택받음.
전체합주기간의 합주시작시간 값은 캘린더에 표시되며, 캘린더에서 날짜를 선택하거나, 설정탭에서 개별적으로 날짜를 지정해 합주시작시간을 날짜별로 다르게 할 수 있도록 하기

권한 카드는 멤버 추가 (+ 아이콘) 과 현재 해당 권한을 가진 멤버를 별개의 아이콘을 배치하여 구분이 용이하도록 할것(사람 아이콘)

팀게시판이나 공지사항에 블라인드 옵션을 추가하고, 댓글 혹은 글 수정권한이 있는 권한자가 타인의 글이나 댓글을 수정할 수도 있는지 확인할것 (삭제, 혹은 블라인드만 가능해야함)
  
모든 뎁스에서 하위 권한자가 상위 권한자를 컨트롤 하지 못하게 할것

팀 이름 설정, 포지션 인원 지정 및 멤버 추가를 한 모달에서 진행 가능하게 할 것
리스트의 아이콘을 통한 수정 및 삭제와 동일한 기능이 모달에서도 가능하게 해야함
 
사이드바의 팀 찾기 메뉴를 팀 관리로, 관리 비권한자에게는 내 팀 메뉴로 보이게 할것.

게시글 페이지의 기능버튼(목록으로, 수정, 삭제 아이콘)이 얼라인 및 UI 완성도가 높게 배치할것.


버그
사이드바 특정 메뉴( 공지사항, 팀게시판)로 이동시 프로필카드 모달 열렸다 닫힘
집중합주기간이 현재 수정만 되고 삭제 버튼이 없음

---

# 결정 완료 — 해야 할 것

결정 근거는 2026-09-15 세션. Cal.com·Skedda·When2meet 분석 결과 반영.
DB 는 밀 것이므로 기존 데이터를 옮기는 작업은 어디에도 넣지 않음.

## 1. 예약 구조를 구간 한 행으로 (다른 모든 것의 토대)

지금은 1시간 칸마다 행이 하나씩임. 10분 단위로 가면 1시간 예약이 6행이 됨.

- `bookings` 테이블을 두고 `starts_at`·`ends_at` 로 구간 한 행. 일정명·예약자·합주실·팀도 여기.
- 선착순은 `(room_id, starts_at)` unique 제약 대신 **구간 겹침 금지 제약**(PostgreSQL `EXCLUDE USING gist`)으로.
  구간이 겹치는 행을 DB 가 직접 막고, 지금처럼 commit 시점에 선착순이 정해짐.
- 진행 중이던 `booking_id`(migration `a7c3f19e4b25`)는 이 구조로 흡수. 별도 컬럼으로 남기지 않음.
- 배정(`assignments`)도 같은 구조로.

## 2. 점유 단위를 설정값으로

`slots.py` 의 `SLOT_MINUTES` 1시간 고정을 걷어내고 권한자가 설정. 예약·배정 전부 그 값을 따름.

값을 **셋으로 나눔**(Cal.com 의 `length`/`slotInterval` 분리를 따르되 격자를 하나 더 둠).

- 예약 길이 단위 — 10분, 30분, 1시간 등
- 시작 시각 간격 — 몇 분 간격으로 시작할 수 있는지
- 화면 격자 간격 — 타임라인에 줄을 몇 분마다 그을지. **1시간 고정**

## 3. 캘린더·타임라인 (직접 구현. MUI X Scheduler 쓰지 않음)

MUI X Scheduler 는 격자·스냅 간격 prop 이 없어 스냅을 어차피 직접 짜야 함. beta 이고
`@mui/material`·emotion 을 통째로 끌고 들어와 스타일 체계가 둘이 됨. 얻는 것이 없어 직접 감.

- `lib/slots.ts` 의 `slotIndex` 가 정수 칸 번호 대신 **소수**를 반환하게. 18:10 → 0.167
- 막대는 소수 위치에 그리고 격자 줄은 정수에만 긋기
- 정수를 전제한 `takenGrid`·`isRangeFree`·`slotCountOf` 가 소수를 받아도 되는지 확인
- 막대 위치·높이는 CSS 퍼센트로

## 4. 예약·불가능 일정 모달 (모달개선예시.png)

3단 구성 — 왼쪽 그날 타임라인, 가운데 등록 폼, 오른쪽 내 불가능 일정 목록과 삭제 아이콘.

- **드래그로 구간 선택**(When2meet 방식). 지금의 시작·종료 드롭다운을 대체.
  모달 한 번 열어 여러 일정 등록하는 요구가 드래그면 자연히 풀림
- 폼 항목 — 날짜, 시작, 종료, 반복, 일정 이름, 사유
- 일정 이름이 캘린더에 표시되는 이름
  - 불가능 일정: 미입력 시 "불가능 일정"
  - 예약: 내 팀 중 하나를 고르는 드롭다운. 미선택이거나 소속 팀이 없으면 예약자 이름

## 5. 예약 이동에 이력 남기기

지금은 지우고 새로 넣어 흔적이 없음. Cal.com 방식으로 옛 행을 취소 표시로 두고 새 행을 만듦.

## 6. 글쓰기 — Tiptap 도입

게시글·공지사항·댓글의 텍스트 칸을 Tiptap 하나로 통일. 볼드·이탤릭·취소줄·폰트·이미지·링크.

- 첨부 파일이 **등록 전에도** 글 안에서 프리뷰됨. 올린 뒤와 같은 모양
- 이미지·링크는 프리뷰, 받아야 하는 사람만 프리뷰 하단 다운로드 아이콘
- 파일 고르는 다이얼로그는 OS 것 그대로 두고 **그걸 여는 버튼만** 디자인에 맞춤

## 7. 밴블릿 링크 임베드

밴블릿 주소를 카톡·디스코드에 붙였을 때 뜨는 미리보기 카드. 라이브러리 문제가 아니라 구조 문제.
Vite SPA 라서 크롤러가 받는 `index.html` 이 비어 있음. FastAPI 가 `index.html` 을 내보낼 때
`og:title`·`og:description`·`og:image` 를 끼워 넣음.

## 8. 집중합주기간 — 전체합주 옵션

**용어는 집중합주기간 그대로.** "전체 합주기간 지정"을 체크했을 때만 내부적으로 팀별합주기간과
전체합주기간으로 나뉨. 체크하지 않으면 지금과 동일.

- 팀별합주기간 = 현재의 집중합주기간 배정
- 전체합주기간 = **그 자체가 일정 하나.** 팀별로 쪼개지 않고 모든 멤버가 같은 하나의 일정을 받음
- 시작 시각과 끝 시각을 둘 다 받음
- 체크하면 팀별·전체를 각각 설정하는 세부 옵션과, 전체합주의 기본 시작 시각 선택이 나타남
- 그 시각이 캘린더에 표시됨. 캘린더에서 날짜를 눌러서, 또는 설정 탭에서 날짜별로 다르게 지정
- **기간이 겹치면 등록을 막을 것.** 겹치지 않으면 달력에 전부 표시(지금은 시작일이 가장 이른 하나만)
- **팀별·전체는 날짜로 분리.** 한 날짜는 한쪽에만 속함(사용자 결정 2026-09-15)
- **전체합주일의 전체합주 외 시간은 선착순 예약**(사용자 결정 2026-09-15)
- **전체합주는 지정한 합주실 1개만 점유.** 나머지 합주실은 평소대로(사용자 결정 2026-09-15)
- **전체합주 날짜 범위는 집중합주기간 안.** 시각은 합주실 운영 시간 안이고 점유 단위 간격 위(사용자 결정 2026-09-15)
- **"매일" 기간은 팀별합주기간 선택을 비활성화하고 전체합주 날짜 범위만 받음.** 그 범위 밖의 날은 매일 팀별 배정. 범위 시작일은 기간 시작일 이후(사용자 결정 2026-09-15)

## 9. 블라인드

팀게시판·공지사항에 블라인드 옵션.

- 블라인드된 글은 **작성자 본인도 못 봄.** 글이 통째로 격리됨
- 권한자가 격리된 글을 모아 보는 화면 필요
- 타인의 글·댓글을 **수정하는 것은 막을 것.** 삭제와 블라인드만 가능

## 10. 팀 색을 팀에 저장 (Radix Colors)

지금은 `--c1`~`--c4` 네 색을 목록에서의 위치로 순환시킴. 팀이 5개가 되면 1번 팀과 같은 색이 되고,
앞 팀을 지우면 뒤 팀 색이 전부 한 칸씩 밀림.

**`@radix-ui/colors`(MIT) 를 씀.** 이름 붙은 색이 25개고 색마다 12단계인데, 단계마다 역할이 정해져
있음. 밝은 화면·어두운 화면 값이 같이 오고 단계 번호의 역할이 양쪽에서 같음.

- **막대 배경은 4~5단계.** 3단계는 거의 흰색이라 색이 안 보임. 파스텔 구간은 4~5단계임
- **막대 글씨는 11단계.** APCA 대비가 보장되어 어느 색을 골라도 글씨가 읽힘

25개 중 바로 옆 색과 거의 같은 다섯(Ruby, Violet, Bronze, Cyan, Grass)을 덜어낸 **20색**.

Tomato, Red, Crimson, Pink, Plum, Purple, Iris, Indigo, Blue, Sky,
Teal, Jade, Green, Mint, Lime, Yellow, Amber, Orange, Gold, Brown

- `teams` 에 `color` 열 추가. 위 20개 이름만 허용(CHECK), **다른 팀이 쓰는 색은 못 고르게 unique 제약**
- 팀 모달(아래 11번)에서 색을 고름. 이미 쓰이는 색은 고를 수 없게 표시
- 고르지 않으면 남은 색 중 하나를 자동으로 줌
- 기존 `--c1`~`--c4` 와 `lib/roster.ts` 의 순서 기반 배정(`c${(index % 4) + 1}`)은 걷어냄
- 불가능 일정에 쓰는 `--off`(회색)는 그대로 둠

## 11. 화면

- 권한 카드에 멤버 추가(+ 아이콘)와 현재 권한 보유자(사람 아이콘)를 별개 아이콘으로
- 팀 이름·포지션 인원·멤버 추가를 한 모달에서. 같은 모달을 여는 문이 둘 — 팀 생성, 그리고
  리스트의 수정·삭제 아이콘
- 사이드바 "팀 찾기" → "팀 관리". 관리 권한이 없으면 "내 팀"
- 게시글 페이지의 목록으로·수정·삭제 아이콘 정렬

## 12. E2E 테스트 복구

시드 스크립트가 지워지면서 `e2e@banblit.test` 계정을 만드는 곳이 없어져 전체가 깨져 있음.
검사가 필요한 계정과 데이터를 스스로 만들게 할 것.

## 13. 버그

- 사이드바에서 공지사항·팀게시판으로 갈 때 프로필 카드 모달이 열렸다 닫힘
- 집중합주기간에 삭제 버튼이 없음(수정만 됨)

## 하지 않기로 한 것

- 소셜 로그인, LLM — 지금 안 함
- 하위 권한자가 상위 권한자를 제어하지 못하게 하기 — 패스
- 팀 겹침 히트맵 — 필요 없음
- Skedda 식 운영 규칙(할당량·예약 창·승인·체크인·space sharing) — 지금 안 함
- MUI X Scheduler — 얻는 것이 없어 도입하지 않음
- 화면 컴포넌트 테스트 — 나중에

## 추가로 결정한 것 (사용자 결정 2026-09-15)

- **마지막 full set 검사의 동시 요청 race 를 보완함.** `permission_service._require_another_full_set` 이
  검사하는 행을 `with_for_update` 로 잠가, 두 요청이 동시에 마지막 full set 을 없애지 못하게 함
- **배정 계산 job 한 번의 최대 실행 시간은 5분.** 넘으면 job 을 실패로 끝냄
- **7번 `og:image` 는 임시 이미지로 먼저 진행.** 정식 이미지는 나중에 교체
